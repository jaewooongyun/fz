# v4.18.0 — Fable 5 재배선: 판단=Fable / 수행=Opus·Sonnet (2026-07-06)

## 개요

Fable 5 제재 해제(2026-07-05) 후 fz의 모델 체계를 3-Tier로 재배선:

```
Fable 5  ─ 판단·결정·관리 (출력 극소 → 2배 단가여도 실비용 ≈0)
  ├─ Lead 세션 (/model fable — B안, 설정 변경 0)
  ├─ 방향 판정 2곳: plan-collaborative stage0-direction/:167 stage0-final
  └─ 검색 수렴 1곳: search-cross-verify stage3-merge (④ pilot 복원)
Opus     ─ 핵심 생산 Primary (플랜 초안·구현·통합) — 불변
Sonnet   ─ 보조 (영향분석·엣지·교차확인·검토) — 불변
```

근거: fable $10/$50 = opus 정확히 2배 — 비용은 output 토큰에 집중되므로 "판단(저출력)=fable / 생산(고출력)=opus·sonnet" 절단선이 비용 최적. 전면 fable(D안)은 가이드 §5 비권장 유지.

## 변경 상세

| 영역 | 내용 |
|------|------|
| 코드 (3줄) | `search-cross-verify.js:166` + `plan-collaborative.js:154`/`:167` `'opus'`→`'fable'` (생산 스테이지 rebuttal/draft/integrate는 opus 유지 — AC-1) |
| 측정 (§5.8) | ④ synthesis 동결→재개 (사전등록 이행 — "해제 시 1줄 전환+재개"가 등록된 경로) · ⑤ direction verdict 신설: N=3, control=retro-baseline(fz-plan #1-2, fable 세션 #3 제외), 12필드 row schema, 미달 시 2줄 원복 |
| lint 신설 | `scripts/lint-model-explicit.sh` — ①model 명시 전수(36 calls, 마커 `agentType:`) ②fable=3 고정 양방향(무단 확산·제거 차단) ③env override 경고. bash 3.2 호환, negative test 2종 + swap 한계 문서화 |
| 문서 (8파일) | guide 제목·배너·§5(B안 가동·C 부분 적용·운용 패턴 3종·Appendix→본문 표제) · team-registry · context-artifacts · governance 모델 상한 행 신설 · fz SKILL 3-Tier·`main: fable`·Lead(F) · skill-authoring :509·§8·§12 model 명시 의무 |

## 검증 사슬

- **plan**: Codex verify needs_revision 5이슈 → plan-v2 반영 (측정 contract·lint·릴리즈 계약 정렬)
- **구현**: code-pair Workflow 11 invokes (full 5 · light 4 · resume 2), agent 21 calls 전부 opus/sonnet — Lead(fable)는 적용·oracle·판정만. 검토 스테이지 실이슈 11건 캐치·반영
- **oracle**: 전 Step grep/node-check/lint/validate 통과 (적용 30/30 edits, anchor 전수 검증)
- **Codex check**: clean — "No discrete correctness issues … lint and workflow syntax checks pass"
- **복원력**: 세션 한도 + 인터넷 중단 2회를 journal resume 캐시로 복구 (stage 손실 0)

## 운용 노트

- 안전 분류기 flag 시 세션이 Opus 4.8로 자동 전환 → `/model fable` 재실행으로 복귀
- fable 에이전트 동시 1 (Lead 세션 제외) · opus 동시 2 — governance 상한 행 + lint가 감시
- 신규 workflow 작성 시 `opts.model` 명시 의무 (§12) — 생략 시 세션 fable 상속으로 비용 2배

## Deferred

- C안 확산: plan stage4-integrate·stage5-recheck / discover merge·landscape / review stage1-arch — ④⑤ N=3 임계 충족 시 별도 사이클
- 워커 effort layering (§5.8 후보 등록만) · de-prescription A/B (③ 대기)
- lint label-set 검사 (AC-5 swap 완전 차단 — openQuestion, ~10줄 검증됨)
- Wave 4 TEAM 일몰: [Unreleased] 유지 (peer-review.js 캘리브레이션 대기)

## 관련

- 이전: v4.14.0(fable 최초 배선) → v4.14.1(제재 롤백) → **v4.18.0(해제 재배선)**
- 작업 아티팩트: `~/dev/TVING/fz-fable-rewire/` (plan-v1/v2·verify-result·progress)
