# v4.14.0 — 전수 주장 오판(Exhaustive-Claim) 방어 + light 모드 검증 경계

> 2026-06-12. MINOR. 23파일 (modules 5 / skills 6 / agents 12). breaking change 0. 신규 모듈/Phase/Gate 0건.

## 배경 사고

api.tving.com Auth-Token 조사 세션(/fz light 라우팅):

1. `rg networkHeaders | head -5` — 잘린 5줄을 전수로 단정 → **"사용처 2곳뿐"** (실제 11곳)
2. `rg "DefaultHeaderProvider\("` — `.init()` 문법 미포착 → 틀린 결론을 "확인"해주는 가짜 교차검증
3. 후속 4턴이 재실측 없이 숫자 재인용 — 오류 생존
4. 사용자 반례 제시("이런 구조면 넣는 거 아니야?")로 발각

**핵심 진단**: 막을 방어(Coverage Gate, fz-search Cross-Verify, T6/T7)가 전부 이미 존재했으나, 40차 light 라우팅이 스킬/모듈을 로드하지 않아 **전량 우회**. "방어 부재"가 아니라 "방어 우회"가 근본 원인.

| RC | 내용 | Fix |
|---|---|---|
| RC1 | 라우팅·트리거 기준이 "요청 어휘"(확인해줘)이지 "산출물 타입"(전수 주장)이 아님 | F1 + F2(a) |
| RC2 | Coverage Gate가 파일 읽기 커버리지만 정의 — 명령 출력 커버리지(grep 잘림) 미정의 | F2(b,c) |
| RC3 | 같은 대화 내 이전 턴 수치 재인용이 어떤 트리거에도 안 걸림 | F3 |
| RC4 | 교훈이 도구 한정("Codex 출력 head 금지")으로 키잉되어 일반 grep에서 recall 실패 | F4 |

## 변경

### F1 — light 모드 검증 경계
- `modules/lead-action-default.md` 40차 row: "full 파이프라인 차단" → "절차 차단 — 단 산출물이 전수/카운트/부정 주장이면 Coverage Gate 유지 (light = 절차 생략이지 검증 생략 아님)"
- `skills/fz/SKILL.md:136` abbreviated recall: 인라인 `Read(modules/cross-validation.md §Coverage Gate)` 명령 — 텍스트 규율이 모듈 미로드로 사문화되는 갭을 Read 명령 직접 삽입으로 차단 (리뷰 Q:Q1 반영)
- `skills/fz/SKILL.md:181` simplified_keywords: 동일 경계 1문장 (466줄 불변 — 행 내부 확장만)

### F2 — Coverage Gate 확장 (`modules/cross-validation.md`)
- **트리거 기준 확장**: 요청 어휘 OR **산출물 타입** ("~뿐/N곳/N개/나머지는/전부") — 리뷰 XA:A1/C:C-2 반영 (이 확장 없이는 F1 지시가 와도 :436 비매칭 단락 가능했음)
- **canonical 선언**: 본 트리거가 단일 정의 — Q-COVERAGE(`lead-reasoning.md`)·fz-search/fz-discover Gate·T8 4곳은 미러 (어휘 drift 방지)
- 절차 5항: 명령 출력 커버리지 — 근거 명령 head/tail 잘림 금지, `wc -l` 병기
- 절차 6항: 분할 합계 검산식 (예: `80 = 58+6+14+2`) — 정규식 불완전(가짜 교차확인)도 검산 불일치로 탐지
- Gate 조건 2항 + BAD/GOOD 1쌍

### F3 — T8 리마인더 (`modules/system-reminders.md`)
- 트리거: 전수/카운트/부정 키워드 + event signal — **(측정 도구 호출 흔적 없이)** 이전 턴 수치 재인용 OR 원 측정 출력 부재 OR 잘림 흔적 (T6 형제 AND 패턴 — 리뷰 Q:Q4/XA:A2 반영, 정상 요약 보고 오발동 방지)
- 적용 범위 명시: 잘림·재인용·측정 부재 클래스 — 정규식 불완전은 F2 검산식 담당 (리뷰 A:A5)
- Backstop 'SOLO 5턴 이하' 예외 비상속 (사고가 4턴 생존)
- 에이전트 12파일: **포인터 레이블 행만** `(T6/T7/T8)` 동기화. 행동 규율 행은 유지 — T8은 멀티턴 Lead 전용이라 1-shot 에이전트에 부적용 (리뷰 A:A1/C:C-1 — dead rule 12곳 재생산 방지). memory-curator는 교훈 회상 역할이라 의도적 제외.

### F4 — 교훈 키잉 규칙 (`modules/memory-guide.md`)
- Write Policy: 교훈은 도구·맥락 한정이 아닌 **실패 클래스**로 서술 — 같은 실패 클래스의 다른 발현에서 recall되도록

### OQ1 — 4 스킬 동기화
- fz-plan/fz-review/fz-code light 섹션 + fz-discover Gate 1: 산출물 타입 조건 1줄씩

## 검증

- **plan**: `plan-collaborative.js` Workflow (9 agents·5 stages·1327s·null 0) — 에이전트 실측이 4-fix 사문화 갭 4건 신규 발견 (:136 abbreviated recall 'ID 목록만' / Q-COVERAGE 이중 진입점 / 에이전트 T8 dead rule / memory-guide :30 충돌)
- **review**: `review-live.js` Workflow (5 agents·724s) — findings 18 (critical 0 / major 8 / minor 10, FP 0), **적용 전 리뷰**가 plan 구조 결함 2건 포착, 18건 전원 수용 → plan-v2 반영 후 적용
- **기계 verify**: 줄 수(fz/SKILL.md 466 불변, 모듈 +α 기대값 일치) / S7' 5중 검증(포인터 12·행동 규율 유지 12·과잉 교체 0·curator 0·파일당 1) / canonical 1정의+4미러
- ⛔ **Codex cross-model 미수행** (quota ~2026-06-28) — 후속 재검증 5항: ① F2 검산식 false-positive율 ② T8 false-positive율 ③ S3 :136 인라인 Read의 실효성 2단(읽히는지+로드되는지) ④ 에이전트 동기화 후 T8 실발동 ⑤ T8 AND 조건 false-negative율

## 산출물 (TVING 워크스페이스)

`~/dev/TVING/fz-gap-exhaustive-claims/` — analysis.md (사고 E1-E5 + RC1-RC4) / plan/plan-v1.md / plan/plan-v2.md (리뷰 18건 반영) / plan/code-context.md
