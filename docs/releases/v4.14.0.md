# v4.14.0 — Claude Fable 5 대응 + 전수 주장 오판(Exhaustive-Claim) 방어

> 2026-06-13. MINOR. 통합 릴리즈 — **Part A** Claude Fable 5 대응 (28 path) + **Part B** 전수 주장 오판 방어 (23파일). breaking change 0. 행 삭제 0.

---

## Part A — Claude Fable 5 대응: 모델 가이드 신설 + 효율 배선 + TEAM 레거시 정리

**Trigger**: Claude Fable 5 GA(2026-06-09) → 공식 문서(Tier 1) 리서치 + fz 가이드↔구현 갭 분석(갭 9건) + 사용자 보완 지시("프롬프트부터 다르게 작성해야 하는 부분 포함") → plan v3.1 (Workflow 9 agents + fresh-context 검증 2회).

### TL;DR

Fable 5를 fz 생태계에 **적재적소로** 배선: 모델 사실은 신규 `guides/fable-model-guide.md`에 단일화, effort는 capability-sensitive 4스킬(plan/review/discover/search)에만 xhigh(측정 큐 동반), 프롬프팅 스니펫은 7종 중 실측으로 빈 곳 1종만 채택(grounded progress — 5 workflows OVERRIDE + VD Brief 4번), Codex quota 공백은 기간 조건부 플래그 + fresh-context 검증자 분기로 메움. synthesis pilot 1곳 fable 승격(사용자 합의). Workflow 전환(v4.12.0) 잔재 "TeamCreate 강제" 4건 문구 교정.

### Highlights

1. **Fable 모델 가이드 + 가이드 갱신**: `fable-model-guide.md` — 사양($10/$50·1M·thinking 상시)/API 차이 표/Claude Code 통합(effort frontmatter·안전 분류기 자동 폴백·서브에이전트 fable enum 환경 실측)/공식 효율 권고/fz 모델 전략 4-axes/스니펫 채택 현황. 모든 [verified:] 주장은 probe-report verbatim 추적 가능.
2. **Fable 효율 배선 — 전부 측정 동반**:
   - fresh-context 검증자(fz-review 검증 2 불능 분기): 본 사이클 3연속 catch 실증(가이드 23건 / plan M2+L4 ×2) — `[fresh-context: claude]` 이종성 상실 태그, model 명시 의무(Fable 자동상속 비용 방지)
   - `effort: xhigh` frontmatter 4스킬 (frontmatter→Workflow 전파 [미검증] — §5.8 ①에서 첫 측정, retro-baseline 설계 포함)
   - grounded progress: "보고하는 주장은 도구 결과/입력 근거 지목 가능해야" — Workflow 에이전트 `[verified]` 오측의 구조 방어
   - synthesis pilot: search-cross-verify stage3-merge `model: 'fable'` — A/B probe에서 fable이 opus 미발견 결함 1건 추가 발견(우위 관찰). 동시 fable 상한 1개 거버넌스 신설
3. **측정 큐 §5.8 사전등록**: ①effort ②fresh-context catch ③절차밀도 A/B(R8 확산 게이트) ④synthesis(활성·pilot 적용 — N=3 확산/롤백 판정). 사전등록 임계 변경 금지.
4. **미적용 결정의 추적성**: 스니펫 비채택 5종·effort medium 강등·전면 fable(D안) — 전부 근거와 함께 기록 (재논의 가능).

### 검증 (Part A)

plan Workflow(9 agents·5/5 stages) + fresh-context 검증 2회(M2+L4 → plan v2/v3.1 정정 — Workflow `[verified]` 오측·승계 Step 교차 모순 catch) + review Workflow(5 agents — findings 7: major 2/minor 5, FP 0, 전원 수용·Reflection 100%) + node --check 5/5 + A/B probe(fable vs opus). 리뷰 major 2건은 동일 실패 클래스 "사후 피드백 반영 시 승계 문서 미동기화" — 3연속 발현으로 후속 개선 후보 등록.

### 산출물 (TVING 워크스페이스)

`~/dev/TVING/fz-modernize-2026-06-12/` (가이드 문서화: probe/audit/verify) + `~/dev/TVING/fz-fable-enhancement/` (analysis.md 갭 9건 / plan v1→v3.1 / code/progress.md / review/self-review.md)

---

## Part B — 전수 주장 오판(Exhaustive-Claim) 방어 + light 모드 검증 경계

> 2026-06-12. 23파일 (modules 5 / skills 6 / agents 12). 신규 모듈/Phase/Gate 0건.

### 배경 사고

api.tving.com Auth-Token 조사 세션(/fz light 라우팅):

1. `rg networkHeaders | head -5` — 잘린 5줄을 전수로 단정 → **"사용처 2곳뿐"** (실제 11곳)
2. `rg "DefaultHeaderProvider\("` — `.init()` 문법 미포착 → 틀린 결론을 "확인"해주는 가짜 교차검증
3. 후속 4턴이 재실측 없이 숫자 재인용 — 오류 생존
4. 사용자 반례 제시("이런 구조면 넣는 거 아니야?")로 발각

**핵심 진단**: 막을 방어(Coverage Gate, fz-search Cross-Verify, T6/T7)가 전부 이미 존재했으나, 40차 light 라우팅이 스킬/모듈을 로드하지 않아 **전량 우회**. "방어 부재"가 아니라 "방어 우회"가 근본 원인.

| RC | 내용 | Fix |
|---|---|---|
| RC1 | 라우팅·트리거 기준이 "요청 어휘"(확인해줘)이지 "산출물 타입"(전수 주장)이 아님 | F1 + F2(a) |
| RC2 | Coverage Gate가 파일 읽기 커버리지만 정의 — 명령 출력 커버리지(grep 잘림) 미정의 | F2(b,c) |
| RC3 | 같은 대화 내 이전 턴 수치 재인용이 어떤 트리거에도 안 걸림 | F3 |
| RC4 | 교훈이 도구 한정("Codex 출력 head 금지")으로 키잉되어 일반 grep에서 recall 실패 | F4 |

### 변경

**F1 — light 모드 검증 경계**: `modules/lead-action-default.md` 40차 row "절차 차단 — 단 산출물이 전수/카운트/부정 주장이면 Coverage Gate 유지 (light = 절차 생략이지 검증 생략 아님)" + `skills/fz/SKILL.md:136` abbreviated recall에 인라인 `Read(modules/cross-validation.md §Coverage Gate)` 명령 (텍스트 규율 사문화 차단) + `:181` simplified_keywords 동일 경계.

**F2 — Coverage Gate 확장** (`modules/cross-validation.md`): 트리거 기준 확장(요청 어휘 OR **산출물 타입** "~뿐/N곳/N개/나머지는/전부") + canonical 선언(Q-COVERAGE·fz-search/fz-discover Gate·T8 4곳 미러) + 절차 5항(명령 출력 커버리지 — head/tail 잘림 금지, `wc -l` 병기) + 6항(분할 합계 검산식 — 정규식 불완전 가짜 교차확인도 검산 불일치로 탐지).

**F3 — T8 리마인더** (`modules/system-reminders.md`): 전수/카운트/부정 키워드 + event signal(측정 도구 미호출 재인용/원 측정 부재/잘림 흔적 — T6 형제 AND 패턴) 감지 시 재실측 요구. Backstop 5턴 예외 비상속. 에이전트 12파일은 포인터 레이블 행만 동기화(dead rule 방지).

**F4 — 교훈 키잉 규칙** (`modules/memory-guide.md`): 교훈은 도구·맥락 한정이 아닌 **실패 클래스**로 서술.

**+ OQ1**: fz-plan/fz-review/fz-code light 섹션 + fz-discover Gate 1 — 산출물 타입 조건 1줄씩.

### 검증 (Part B)

plan Workflow(9 agents·5 stages·1327s — 사문화 갭 4건 신규 발견) + review Workflow(5 agents·724s — findings 18: major 8/minor 10, FP 0, **적용 전 리뷰**가 plan 구조 결함 2건 포착, 전원 수용 → plan-v2 반영) + 기계 verify(줄 수/5중 검증/canonical 1정의+4미러).

### 산출물 (TVING 워크스페이스)

`~/dev/TVING/fz-gap-exhaustive-claims/` — analysis.md (사고 E1-E5 + RC1-RC4) / plan/plan-v1.md / plan/plan-v2.md / plan/code-context.md

---

## 공통

⛔ **Codex cross-model 검증 미수행** (quota 차단 ~2026-06-28) — Part A는 fresh-context 검증자(F3 배선이 자기 검증에 사용된 dogfooding) + Workflow 다각도, Part B는 Workflow 다각도 + 기계 verify로 대체. 회복 시 후속 재검증: Part A(v4.14.0 diff 이종 리뷰 + F2 플래그 원복) + Part B(검산식/T8 false-positive율 등 5항).
