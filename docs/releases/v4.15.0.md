# v4.15.0 (2026-06-18) — 외부 리뷰어 catch 환류 회로 (promotion-ledger 트랙 C) [MINOR]

## 배경

ASD-1793 PR에서 CodeRabbit이 fz-plan/code/review가 미탐한 이슈(retain cycle 등)를 catch했다. 4-lens Workflow 분석 결과, 근본 원인은 **fz의 능력 부족이 아니라 "환류 회로"의 단절**이었다:

- **입구는 존재**: `pipelines.md` #19 pr-comment-review (트리거 `코드레빗|PR.*코멘트`)가 외부 코멘트를 받아 classify-all → verify-each → user-confirm으로 처리.
- **출구가 단절**: 그 결과가 Issue Tracker / promotion-ledger 관측 / lens candidate 등록으로 흐르지 않음 → 외부 발견이 fz 자기개선으로 환류되지 않음.

3겹 진단: (A) Codex 이종 안전망 다운 기간 동종 폴백 → 분포-의존 결함 미탐, (B) closure-capture retain cycle lens가 Claude 경로에 부재(Codex `fz-reviewer`엔 존재), (C) plan-anchored review 맹점.

## 변경 (adopt-now)

분류축: **"코드를 flag하는 검출 rule인가?"** → Yes면 evidence 룰(memory-guide:45) 적용 defer, No(인프라/배관/advisory)면 개별 평가.

### 1. promotion-ledger 트랙 C (`modules/promotion-ledger.md`)
- Eligible Session에 조건 (c) 추가: 외부 도구(CodeRabbit/팀원/Codex)가 `/fz-review --deep` 이후 actionable Major+ 1건+ 발견 세션. 4-classify에서 `project-rule`/`valid-suggestion`만 카운트 (preference/needs-review 제외 → precision ~55% FP 차단).
- 관측 기록 형식에 `finding-source: internal | external({tool})` 필드 추가.
- 2-트랙 구분에 트랙 C 정의 추가 (활성 전환은 트랙 A 5-session 기준 준용).
- **P2-C 관측 #0 등록**: general closure-capture retain cycle lens (ASD-1793 CodeRabbit Major1). ⛔ evidence 1세션 → candidate, 활성 차단 (memory-guide:45).

### 2. pr-comment-review #19 펜 (`modules/pipelines.md`)
- 체인에 `import-to-ledger` 추가. user-confirm 후 절차 4: valid/needs-investigation 승인 항목이 `/fz-review --deep` 이후 외부 fz-miss면 → Issue Tracker append + 트랙 C 관측 기록. user-confirm 게이트가 기록 전 사람 확인 보장. fz-miss 아니면 skip.
- 이것이 트랙 C를 작동시키는 트리거(펜). 없으면 ledger는 빈 장부.

### 3. fz-review Codex 불능 폴백 보강 (`skills/fz-review/SKILL.md` 검증 2 분기)
- **rank3b**: fresh-context Claude 검증자가 retain cycle 검사 시 `codex-skills/fz-reviewer/SKILL.md` Memory Management 체크리스트 명시 적용 — Codex 부재 시 이종 parity 복원 (저장 프로퍼티 보유 closure·completion handler·Rx subscription 포함, View 한정 아님). 이번 세션 miss(A+B 교집합)의 직접 처방.
- **rank6**: PR open 시 pr-comment-review를 보조 이종 소스로 활용 가능 안내 (Lead 판단).

## Deferred (검출 rule — evidence 1세션, 트랙 C 누적 후 승격)

- **rank3a**: `safety-audit.md` 검증 4-J에 Grep 기반 general closure-cycle 검출 → Grep FP 높음 + 패턴 정교화 선행. 트랙 C P2-C 3세션+ 누적 후 active.
- **rank4**: `review-live.js` Stage1 optional planPath + plan-critique → 올바른 plan FP. 1~2세션 실측 후.
- **rank5**: fz-peer-review 문서화 입구 → 펜은 rank2가 담당(자연 이벤트), secondary.

## Rejected (재제안 방지)
- 라인레벨 방어 직접 lens화 (evidence 0, FP) · memory-leak-hunter L3 (Codex 중복) · fz-review 단계 plan 역방향 재비판 (소급 FP) · plan-blind 강제 (~55% FP).

## 가이드 준수
- additive only, 행 삭제 0 [prompt-optimization.md:153-167 트리밍 비저하].
- 검출 Grep rule 신설 0 (rank3b는 기존 fz-reviewer 체크리스트 *참조*, 신규 rule 아님).
- CRITICAL/MUST 신규 0 [prompt-optimization 원칙8] (⛔ 구조 마커 +1 = 기존 컨벤션 일관). promotion-ledger 131줄(≤200).
- **fz-review SKILL.md 557→560줄** (선존 500 초과, +3 load-bearing — 모듈 분리는 별도 트랙, 트리밍 비저하상 Gate/Few-shot 삭제 금지).
- 펜 옵션 A(자동) 사용자 확정.

## 검증
- ⛔ Codex cross-model verify 미수행 (quota ~2026-06-23 리셋). 가이드 grounded + anchor probe 전수 검증(promotion-ledger / pipelines #19 / fz-review:216 / fz-reviewer:35-36) 대체. quota 회복 시 후행 check.
- 산출 plan: `~/dev/TVING/fz-codereview-gap/plan/plan-v2.md`.
