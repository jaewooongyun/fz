# v4.39.1 (2026-09-25) — 마지막 검증이 마지막 편집보다 앞섰다 [PATCH]

> v4.39.0 발행 뒤 재리뷰(결정론 검사 + GPT 1패스)가 찾은 문서 사실 오류를 고친다. 동작 변경은 없다.

## 1. 무엇이 문제였나

- **노트를 게이트 재실행 뒤에 줄였다.** v4.39.0 노트를 5절로 줄이면서 형식 기준을 형제 v4.38.0 노트로 잡았는데, 그 노트 자체가 `docs/releases/_TEMPLATE.md` 밖이었다. 템플릿 게이트 B8 이 깨진 채 발행됐고, 검증 절은 "게이트 34/34 재실행"(실제는 자동 33 + 수동 1)을 적고 Reflection 70% 가 임계 80% 미달이라는 사실을 뺐다
- **실측한 사실을 가이드가 모른다.** effort 를 빼면 워커가 세션 값이 아니라 모델 기본(Opus 5.5 = `medium`)으로 돈다는 것을 v4.39.0 작업이 실측했는데, 가이드 4곳은 "세션 상속" 이라 적고 있었다
- **요약이 없는 주장을 만들었다.** README 의 v4.38.0 한 줄 요약이 두 문장을 합치며 "다섯 곳을 전부 health-check 에 배선" 이 됐다 — 다섯 곳 중 Gate 4.7-S·`FZ_AUTONOMY` 는 health-check 대상이 아니다
- **롤백 스크립트가 기본처럼 읽혔다.** `plan-collaborative.js` 를 현재 실행 경로처럼 적은 문장 2곳

## 2. 무엇을 고쳤나

| 항목 | 이전 | 이후 |
|---|---|---|
| v4.39.0 노트 | 5절 · 검증 수치 오류 | 템플릿 6절 · 게이트·Reflection·커밋 무결성을 사실대로 · 미검증 항목 복원 · advisor 관측 분모 병기 |
| effort 생략 서술 | "세션 상속" 4곳 | 워커 모델 기본 — skill-authoring §12 · model-guide 2곳 · lint 주석 |
| plan 스크립트 | collaborative 가 현재 실행처럼 읽힘 | 기본은 `plan-lean2.js`, collaborative 는 롤백 경로 (agent-team-guide · fz-plan) |
| README | v4.38.0 요약 오류 | 원 노트 서술대로 |
| CHANGELOG v4.39.0 | "Lead 만 advisor 를 쓴다" | 워커 호출은 프롬프트로 억제 · 관측 집단 0회 |

## 3. 하지 않은 것

- **v4.39.0 커밋 이력** — 다시 쓰지 않는다. 10커밋 중 5개가 단독 health-check 에 실패하지만(`F-310`) main·태그가 최종 상태를 가리키고 이미 발행됐다
- **v4.39.0 태그 안의 노트와 GitHub Release 본문** — 발행 당시 그대로다. 고친 판은 main 의 `docs/releases/v4.39.0.md` 다
- **재리뷰 제안 4건** — 래퍼의 `--add-dir` 안내 정리 · "P1 sweep 이후" 조건 · 플래그 검사의 빈 대상 확인 · 게이트 환경 변수 부재 분류. 이번 범위 밖이다

## 새 도구

없음 — 문서만 고쳤다.

## 검증

- 커밋마다 격리 clone 에서 `health-check` exit 0 — v4.39.0 의 커밋 무결성 누락(`F-310`)을 이번 분할에 적용
- v4.39.0·v4.39.1 노트가 템플릿 6절을 모두 갖는다 · 버전 3곳 일치(`DOCS_SYNC_OK`) · GPT 표면 검사 위반 0
- 노트 수치 재대조: self-test 53/53 · 22/22 · 24/24 · 7/7 (재실행)

---

## ⛔ `Closes:` 줄 규약

닫은 finding 이 없어 `Closes:` 줄을 쓰지 않는다. `F-310`·`F-311` 은 절차 교훈이라 문서 수정으로 닫히지 않는다.
