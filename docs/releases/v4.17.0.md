# v4.17.0 (2026-06-29) — 가이드 모더나이제이션 + 가이드 준수 remediation [MINOR]

## 배경

외부 최신 권위 자료(공식 docs + arxiv)로 가이드를 모더나이제이션하고, **그 갱신된 가이드를 기준으로 플러그인 전체(skills · agents · modules · guides)를 감사·정합화**한 세션. 모더나이제이션 → audit → remediation(①②③④) → 다각도 리뷰의 검증 사슬로 진행했고, 각 단계가 직전 단계를 교정했다(audit이 모더나이제이션의 소비처 전파 누락 catch, 리뷰가 remediation의 calibration 과교정 catch).

⛔ Codex cross-model verify 미수행 (quota ~2026-06-30 리셋). 검증층은 동종 fresh-context Claude Workflow(리뷰 22 agent, 5차원 + adversarial verify) — **이종 안전망 상실** 명시, 회복 시 후행.

## 모더나이제이션 (guides)

- **`llm-references.md` 신규**: Tier 1(공식 docs) / Tier 2(arxiv 16 실증) / Tier 3(커뮤니티 supporting-only) 출처 + anti-pattern + deprecated 정책 + fz 매핑.
- arxiv 16건 전수 실증(API resolve) → 미검증→verified 태그 교정, 제목·번호 정정.
- **MAST**(arxiv 2503.13657 "Why Do Multi-Agent LLM Systems Fail?", Cemri) 인용 복원 → modules 재인용으로 active 환원.
- **Opus 4.8 단일화** — 최신 버전만 유지, 이전 버전 대응 제거. fable은 frozen(제재 미운용) 캐비엇.
- skill-testing §6.4(리뷰형 2단계 coverage/verification) + §4 표 형식 허용 명시.

## Remediation (가이드 준수 — audit 도출)

### ① self-inflicted 전파 (modules)
Fable frozen 캐비엇(team-registry·context-artifacts), Bucket 재앵커, MAST 인용(complexity·cross-validation), Fable 보조절 제거(team-core·lead-action-default). 모더나이제이션이 canonical(fable-model-guide)만 고치고 소비처 전파를 누락한 갭을 audit이 catch.

### ② when-not 라우팅 (17 skills)
17 user-invocable description에 "비사용: X →fz-Y" 1절 — mis-route 방지(자체 벤치마크가 FAIL로 잡던 중복쌍 위험 차단). fz-fix surgical 원칙.

### ③ Workflow 전환 일관성
fz 자기모순 해소, agent-team-guide §3 전환 노트, agents/*.md 5 P2P 적용범위(TeamCreate 경로 보존), build.md UI visual-oracle 게이트.

### ④ test-spec + eval + Few-shot + hooks
- **test-spec 17/17**: Workflow fan-out(15 agent) 생성 → Option A 인라인 10 + Option B references/ 7.
- **eval §6.4**: fz-skill 리뷰형 2단계(coverage recall ≥90 / verification precision ≥80) + Gate.
- **Few-shot 5스킬 ≥3쌍**.
- **`examples/hooks.json.example`**: opt-in 템플릿(active 배선 0, 설치=팀합의). harness "Hooks opt-in·런타임 미적용" 정직화.

## 다각도 리뷰 (--team --deep)

- 22 agent / 5차원(가이드준수·test-spec정확성·surgical·일관성·calibration) + finding별 adversarial verify(refute-default).
- **16 findings → 확정 8(전부 minor/nit, critical/major 0) / 반증 8**. 반증층이 리뷰어 자신의 과장·오인용까지 교정(23차 부분 완화 — 단 동종이라 진짜 이종 아님).
- 반영: **CAL-1**(fz-manage·fz-pr-digest 외부화 → Option B 7, 500 버퍼 복원), **D1-4**(eval §6.4 precision 용어 일치), **CAL-6**(§9 비면제 정정), D4-7·D1-6·D2-6·CAL-5.

## calibrate 교훈 (35차)

Option A/B: plan "+50줄→8 외부화" 추정이 실측 spec(~22-35줄)과 달라 1차 5, 리뷰 CAL-1로 7 수렴. **calibrate-from-real이 안전버퍼까지 깎을 수 있음**(>480→>500 완화) — 하드 제약 충족 ≠ 안전 마진.

## 가이드 준수

- AC#1: remediation 신규 추가분 작업맥락(티켓/날짜/N차) 0.
- 전 SKILL.md ≤500(최대 fz-discover 499), 17/17 test-spec 헤더, Option B references 7개.

## 산출물

- `fz-modernize-2026-06-28/`(probe·gap-verification), `fz-plugin-audit-2026-06-28/`(audit-report·plan·code·review) 로컬 보존.
