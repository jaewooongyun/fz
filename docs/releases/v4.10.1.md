# Release v4.10.1 (2026-06-02)

**Title**: 하네스 self-maintenance + figma candidate 신호
**Type**: PATCH
**Trigger**: v4.10.0 이후 누적된 8 커밋의 유지보수 배치 정리. ASD-1718 figma 실측 실패 회고(`TVOD/ASD-1718/retrospective/`)를 fz 플러그인 고도화 플랜(TEAM plan + 2× review)으로 환류 + ASD-1674 회고 candidate 신호 + hot-path 슬림화 + 모델 세대 갱신.
**Cost note**: TEAM plan + 구현 리뷰 + 릴리즈 영향도 평가 모두 Codex 제외(사용자 결정 + candidate 추가는 C-17상 Codex 불요). candidate→active 승격용 Codex verify는 PENDING(할당량).

## TL;DR

active 기능 0의 순수 유지보수 릴리즈. 두 `feat` 커밋은 `5-session 관측 후 활성` gate 뒤 **비활성 candidate 신호**이고, `feat(scripts)`는 inbound 참조 0의 dev-only 측정 도구라 플러그인 사용자가 관찰하는 동작 변화가 없다. 나머지는 fix/docs/refactor/chore. breaking change 0, regression risk none(파괴적 커밋 2건 behavior-preservation 실측 검증). 따라서 **PATCH** (선례 v4.5.1 [PATCH] 순수 유지보수 릴리즈와 동일 프로파일 — 이 repo는 기계적 feat→minor 규칙 없음).

## Highlights

### 1. figma candidate 신호 강화 (ASD-1718) — `f3f6a5a`

ASD-1718 회고에서 사용자가 figma 실측으로 9건 catch. 기존 `fz-code` `figma 수치 미측정` candidate 신호가 *수치(spacing/margin 등)*만 커버해 색·정렬·텍스트·z-order 실패를 못 잡던 gap을 보완.

```
fz-code/SKILL.md:277 (in-place 확장):
  trigger += 색(node fill) / 텍스트 style-run({tsN}) / 정렬(alignItems·justifyContent) / 컨테이너 gap 별도 측정
  action += figma raw 구조 데이터(fills·opacity·childOrder=z-order) > render 눈대중 override 금지
  42차 caveat → "구조 데이터 부재(flattened IMAGE/SVG)" 한정으로 재범위화
신규 row: figma 텍스트 미대조 [candidate: 2 session evidence] (1674#2 + 1718#5 2세션 재발)
promotion-ledger L-1 관측 #2 (ASD-1718, 트랙 A 2/5)
```

> 비활성 candidate — `강제 예외 / 5 sessions 관측 후 활성 결정`. 릴리즈 시점에 동작 변화 없음.

### 2. figma 회고 candidate 신호 3건 (ASD-1674) — `f9dc847`

`fz-code` friction-detect에 candidate 신호 3건(기존 인프라 미확인 helper / 표면 churn / figma 수치 미측정) + `promotion-ledger` L-1~L-3 관측 #1. 트랙 A 신호활성 / 트랙 B 메모리승격 2-lifecycle 구분 도입.

### 3. hot-path 슬림화 + cleanup (⚠️ destructive — 검증 완료)

```
refactor(agent-team-guide) f0a63ee  −103줄: §3 5패턴 pseudocode 중복 → 매핑 표 + patterns/ 포인터 (550→457줄)
  검증: operative는 modules/patterns/ 5파일(collaborative/pair-programming/live-review/adversarial/cross-verify)에 보존 — 모두 선존·더 풍부, f0a63ee가 patterns/ 미변경
chore(cleanup) 25b5e0e  −50줄: plan-tradeoff.md.archived + team-registry 취소선 행 제거
  검증: 전 repo active 참조 0건 (잔존 4건은 역사적 CHANGELOG/release-note 인용)
```

### 4. maintenance

```
fix(cross-validation) 0a9c3b0: Reflection Rate 계산식 schema 일원화 (계산식 불변, 권한 분리 정정)
docs(opus48)          20bc442: Opus 4.7→4.8 stale 참조 갱신 ([미검증] 태그·역사 citation 보존)
feat(scripts)         fa73cf2: measure_constraint_load.py 신규 (COST 축 ablation 도구, 0 inbound 참조)
chore(gitignore)      3c73fc9: .fz-work/ + experiment-log-traces.jsonl 무시
```

## 영향도 / 리스크

| 항목 | 평가 |
|------|------|
| Breaking change | 없음 (public skill/agent 인터페이스 제거 0) |
| Regression risk | none — 파괴적 커밋 2건 behavior-preservation 실측 검증 |
| 사용자 동작 변화 | 없음 (feat = 비활성 candidate, script = dev-only) |
| Codex verify | candidate→active 승격 게이트 PENDING — 릴리즈 blocker 아님 |

## 검증

- 릴리즈 영향도: 3-analyst 워크플로우(semver 분류 + 파괴적 커밋 regression 검증 + 적대적 버전 도전) → v4.10.1 PATCH 수렴, risk none.
- 산출물 경위: `TVOD/ASD-1718/fz-enhancement/` (plan.md / review.md / implementation.md / review-impl.md).

> 전체 변경 이력: [CHANGELOG.md](../../CHANGELOG.md)
