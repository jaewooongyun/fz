# v4.14.2 — fz-code 구조 평가 convention 면제

> 2026-06-16. PATCH. fz-code `관찰 보고 의무`가 Clean Architecture 위반을 보고할 때, 코드베이스 컨벤션(3곳+ 반복 패턴)을 위반으로 오보고하지 않도록 convention 면제 + same-RIB DI 중복 예외 추가. breaking change 0 · 신규 row 0(기존 row MERGE).

---

## Trigger

사용자 피드백: "요구사항 해결 시 주변 코드 일관성은 챙기나, Clean Architecture/RIB 구조 정합성·확장성 평가가 약하다 (예: ① View에 비즈니스/상태/열거형 로직, ② 상위 DI 무시 로컬 UseCase 생성)." + "특정 상황 일반화=과적합 경계" 명시.

## 진단 (3소스 검증 — 증거에이전트 3 + adversarial challenger + Codex gpt-5.5)

- **modality 비대칭 실재**: 구조 지식(CLAUDE.md/ai-guidelines/clean-architecture.md/review-arch)은 존재하나 passive·사후이고 빠른 경로(fz-fix light/SOLO)에서 누락. restraint는 능동·절차·메모리연결로 무게중심 기움.
- **단 예시②는 결함이 아니라 코드베이스 컨벤션**: 56 Interactor 중 27%가 로컬 UseCase 생성 + 다수 Builder도 동일. Codex Q1 판정: "전역 금지할 결함 아니라 혼재 관행". ContentDetail은 Builder:94 정의 + Interactor:239 재생성이 같은 RIB서 공존 = DI 중복.
- **ledger 모순**: 프로덕션 구조판단 사례 41/45차는 구조 OVER-제안 실패 → 하드룰은 과적합·진자역행(challenger 치명적결함 + Codex Q2 YES).

## 결정

| 결정 | 선택 | 근거 |
|------|------|------|
| 처방 형태 | flag-only + convention 면제 (하드룰 ❌) | challenger 생존안 + Codex Q3 (b) |
| 구현 방식 | 기존 fz-code:233 row MERGE (신규 row ❌) | skill-authoring §3 DELETE/MERGE-default (additive 금지) |
| CLAUDE.md 하드룰 | 미적용 | 27% 컨벤션 위반 flag = 과적합 |
| fz-fix/review-arch 빠른경로 배선 | 미적용 | 40차 light-mode 충돌 |

## 변경 (1파일)

- `skills/fz-code/SKILL.md:233` 관찰 보고 의무 row MERGE: "동일 패턴 코드베이스 3곳+ → convention 간주 보고 생략(예: 로컬 UseCase 생성) + 같은 RIB scope에서 Component가 주입 제공하는 dependency를 Interactor가 동일 재생성 시 convention 무관 보고".

## 검증

- **Codex cross-model verify** (gpt-5.5 high, **quota 복구 후 실가동**): verdict needs_revision → 3보완 반영 (① same-RIB 예외 wording 좁힘 ② behavioral probe SC5/SC6 추가 ③ 잔여리스크 명시).
- **behavioral probe** (결정론적): SC5 로컬 UseCase 27%(15 Interactor) → 미보고 PASS / SC6 ContentDetail same-RIB 중복 → 보고 PASS.
- **benchmark 비저하**: fz-code 403줄 불변(table row=단일 line), static 8항목 미접촉. 신규 row 0(35 row 유지).

## 잔여 리스크 (Codex Q2/Q3 — 수용)

- 나쁜 패턴이 3곳+ 널리 퍼지면 convention 면제가 과소보고 → 별도 리팩토링 티켓 사안.
- SOLO 저복잡도 버그 수정은 여전히 구조 회귀 미포착 → 40차 light-mode trade-off로 수용.
- 본 변경은 recall 확장이 아닌 **narrow calibration** (Codex Q4).

## 공통

✅ **Codex cross-model 검증 수행** — quota 차단(~2026-06-28 예상)이 조기 해제되어 이번 릴리즈부터 cross-model 재가동. 이전 v4.12.1~v4.14.1의 "Codex 미수행" 표기 종료.

산출물 (TVING 워크스페이스): `~/dev/TVING/arch-elevation-analysis/` (synthesis-final.md, plan/plan-final.md, codex verdict 2건).
