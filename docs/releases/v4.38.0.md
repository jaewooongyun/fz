# v4.38.0 (2026-09-22) — 검사기는 있었고 부르는 곳이 없었다 [MINOR]

Closes: F-031-candidate-signal-never-promoted-no-ledger-entry, F-204-sc-command-namespace-mismatch-83-sites, F-205-agent-tools-serena-name-mismatch-workers-never-had-serena, F-211-lsp-tool-names-drift-four-skills, F-285-wiring-traced-symptom-never-observed, F-286-sibling-conformance-preempts-consolidation, F-288-absolute-path-habit-misattributed-tool-absence-as-failure, F-289-inventory-checked-in-one-document-while-two-declare-it, F-290-self-test-aborts-on-rejection-expecting-fixture

## 문제

규칙·검사기·플래그가 존재하는데 **호출처가 0건**인 결함이 다섯 번 나왔다.
`check_release_sync.sh` · `fz_wf_metrics --self-test` · `plan_integrity_check.py` · Gate `4.7-S` · `FZ_AUTONOMY`.
부르지 않는 검사는 실패하지 않으므로 아무 경고도 없었다.

## 원인

- **대리 오라클** (`F-292`) — 완료 근거와 측정 대상이 달랐다. `/sc:` 치환을 `mcp__serena__` 0건으로 판정했고, 재측정하니 86곳 중 1곳이 빠져 있었다.
- **한 방향 대조** (`F-293`) — 0건만 의심하고 큰 수는 의심하지 않았다. 교훈 번호 집계가 `5차원` 까지 긁어 18종/91회를 23종/184회로 부풀렸다.

## 조치

| 영역 | 변경 |
|---|---|
| 배선 | 검사기 신설 15 · 수정 6, 전부 `health-check` 에 등재 |
| Workflow | `mode:'fallback'` 을 SOLO 로 직행시키지 않고 판별 표 6종으로 분기 |
| 리뷰 | 4.7-S 증상 관측 · 형제 관례 양방향 · 단일 도구 0건 삭제 권고 금지 |
| 게이트 | `TOOLS` — 도구 부재는 FAIL 이 아니라 UNRUN |
| 단일 출처 | `schemas/tool-inventory.json` 을 lint 와 외부 명령 검사가 공유 |
| 레지스트리 | P-ID `P2-D`~`P2-I` 매핑, candidate 만료 결정표 (3개월 ≠ 90일) |
| 사료 | `team-core.md` · `patterns/*.md` 를 `docs/history/` 로 격리 |

모든 검사기는 self-test 와 어블레이션(수정을 빼도 통과하면 헛돌이)을 거쳤다. 헛돌이 픽스처 7건과 도달 불가 로직 3건을 제거했다.

## 검증

- 기능 단위 14커밋, 커밋마다 격리 트리에서 `lint_contracts` 위반 0건
- 최종 HEAD: `health-check` exit 0 · 버전 3곳 일치 (`DOCS_SYNC_OK`)

## 제약

- D1(resume 보존)은 실제 Workflow 실측이 필요해 이월
- SG4(확신도 자동 진행)는 별도 승인 전 미착수
- `FZ_AUTONOMY` 는 `/fz` Phase 4 에만 배선. 개별 스킬·워커는 fail-closed 로 미배선
- 에이전트 정의 변경은 새 세션부터 반영
