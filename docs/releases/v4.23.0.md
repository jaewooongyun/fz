# v4.23.0 — 누적 통합: 계약 lint 결정화 · 리뷰 구조 판정 축 · llm-references §1.1b (2026-08-10)

## 배경 — 왜 하나의 릴리즈인가

v4.22.0(2026-08-08) 발행 이후 CHANGELOG에 초안 번호 `4.23.0`·`4.24.0`·`4.25.0` 세 섹션이 쌓였다. 그 번호들은 **태그·GitHub Release 어디에도 존재하지 않는다** [verified: `git ls-remote --tags origin | grep -E 'refs/tags/v4\.2[0-9]'` → v4.20.0·v4.21.0·**v4.22.0**뿐 · `gh release list` Latest = **v4.22.0**].

따라서 초안 번호 세 개를 폐기하고 **누적분 전체를 v4.23.0 하나로 발행**한다. 근거:

- 미발행 구간에 `[MAJOR]` 0건 — 전부 MINOR → 합산해도 MINOR 1회 = `4.22.0 → 4.23.0`이 semver 정합
- `4.24.0`/`4.25.0` 문자열은 `plugin.json`·`marketplace.json` 2곳에만 존재 [verified: 리포 전수 `grep -rn '4\.2[45]\.0' --exclude-dir=.git .` — 나머지 히트는 전부 CHANGELOG 초안 헤딩과 v4.22.0 릴리즈 노트의 *이전 사이클 기록*]. 스킬·모듈·가이드·워크플로·훅·스크립트 참조 0건 → 번호 정리가 다른 계약을 깨지 않는다
- 하향(4.25.0 → 4.23.0)을 관측하는 소비자가 없다. 마켓플레이스 소스는 GitHub repo이고 그 최신은 4.22.0 — 외부에서는 `4.22.0 → 4.23.0` 전진만 보인다

CHANGELOG는 세 초안을 **§A·§B·§C 하위 절로 보존**한다(본문 삭제 0줄). 아래 §1~§5가 §A, §6이 §B, §7이 §C에 대응한다.

이 릴리즈의 본체는 **전수 자기 감사**다. 138자산(스킬 21 · 에이전트 13 · 모듈 46 · 가이드 9 · 워크플로 6 · 스키마 5 · 스크립트 · 템플릿)을 대상으로 F-0~F-25를 확정하고 W0~W3b 범위를 반영했다. 방향은 **C(정본) → B(기계화) → A(baseline 소비)**. 산출물: `~/dev/TVING/fz-plugin-audit-2026-08-09/`.

---

## 1. `scripts/lint_contracts.py` 신설 — 언어 지시 17항목의 결정화

**문제**: `/fz-manage check`의 17항목이 **전부 언어 지시**였고 check용 스크립트가 0개였다. 정의된 검사(깨진 참조 · 모듈 목차)가 존재하는데도 위반이 생존했다 — `guides/skill-authoring.md` §11(*"결과가 binary(pass/fail)인가? → 스크립트"*)의 미이행이다.

- ⛔ **스크립트가 항목 SSOT** — SKILL.md는 표를 재정의하지 않고 `--list`에 위임한다. 이중 정의가 F-5·F-19의 원인이었다.
- **24항목 / DETERMINISTIC 15 · THRESHOLD 3 · SEMANTIC 6**. ⛔ 손으로 세지 말고 `--list` 출력을 전사한다 — 감사 중 23/15/3/5로 stale했고, `#15` 삭제 후 25/16으로 **또** stale했다.
- ⛔ **exit 3분**: `0`=위반 없음 / `1`=위반 / `2`=**configuration·parse error**. 루트는 `Path(__file__)` 앵커(CWD 비의존). stdlib 전용.
- `fz-manage check`가 `lint_contracts.py` + **기존 `lint-model-explicit.sh` + `lint_doc_freshness.py`** 를 함께 호출한다 — v1 초안은 새 lint만 만들고 기존 lint 배선을 빼먹어 F-2를 남겼다(Codex 단독 발견).
- ⛔ **"차단" → "검출(요청 시)" 정직화**: 훅 미설치 상태에서 lint는 차단하지 않는다. 실제 차단은 `settings.json`(사용자 소관).

첫 실행 129건 → 탐지기 4회 교정 → **위반 0건**. 교정 근거는 전부 *히트를 열어본 것*이다: `#13`은 `agents/*.md`의 `CLAUDE.md ## Architecture`가 **소비 프로젝트**를 뜻해 89건 오탐 → SEMANTIC 강등 · `#N4`는 마크다운 표의 `\|`(파이프 이스케이프)와 자기 설명 문구 제외 · `#N6`은 `$0`·`git rev-parse --show-toplevel` 앵커도 유효.

### 1-a. 계측기가 자기 유효성을 증명한다 — 양성 대조 하네스

⛔ **가장 중요한 발견은 이 릴리스의 enforcement 계측기 자신이 vacuous했다는 것이다.** "위반 0건 exit 0"이 깨끗함의 증거가 아니었다.

- `hits`는 *본 후보 수*라 패턴이 고장나도 0이 아니다 → `OK [검사 대상 13]`이 찍혔다.
- **fixture 46건 + 통합 5검사(9 위치)** 가 매 실행 선행한다. fixture 실패는 **exit 2**이고 "위반 0건"으로 읽히지 않는다. `--self-test`로 단독 실행 가능.
- ⛔ 범위는 **정규식이 판정의 전부인 항목에 한정** — 구조 순회형은 파싱 실패가 이미 ParseError로 드러난다(`harness-engineering` §6 AP1 과도한 구조화 회피).
- 의도적 회귀 5종 — 무조건 `[]` 반환 · 판정 반전 · 확장자 오필터 · 디렉토리 오제외 · `([], 그럴듯한 히트 수)` — **전부 exit 2로 검출**된다. 통과만 확인한 게이트는 무용하므로 발화 가능성을 함께 실측했다.

### 1-b. 통과가 곧 발화 가능성이 되도록 — 개별 검사 교정

- **`#N6`**: 파일 전체 문자열 검색이라 ① `__file__`이 **주석에만** 있어도 앵커로 인정 ② 무관한 `exit 2`가 fail-closed 가드로 인정 ③ 검사기 자신이 **자기 정규식 정의줄**로 통과했다. ⛔ **본 릴리스가 추가한 `codex-exec.sh`가 7번째 줄 주석의 "exit 2"로 통과하며 자기증명했다.** `chk_N6`와 fixture가 **동일 함수 `n6_ok`** 를 쓴다(판정 드리프트 차단).
- **`#N1`**: `critical` 포함 enum을 정규식으로 찾고 못 찾으면 `continue` → **consumer에서 critical을 삭제하는 변경**(가장 중요한 양성 대조군)이 위반이 아니었다. 구조 순회로 바꾸고 **정의 부재도 위반**으로.
- **`--only` 검증**: 알 수 없는 id → **exit 2**. 이전엔 `--only DOES_NOT_EXIST`가 검사 0개를 돌리고 "위반 0건 exit 0"을 냈다 — 오타 하나로 enforcement 전체가 조용히 무력.
- **`#N2` 확장**: `scripts`·`agents`·`workflows` 카운트 등재 → 즉시 `scripts/ 선언 6 ≠ 실측 7` 검출(**한 세션 안에 만든 stale**).

### 1-c. 사람 검사로 두 번 놓친 것의 기계화

- **`#N7` 셸 변수 정의-사용 불일치**: `get_codex_skill()` → `get_codex_skill_path()` 전환에서 **할당은 `_SKILL_PATH`, 조건문은 옛 `_SKILL`** 인 스니펫이 3곳 남아 `[ -n "$미정의" ]`=false → **searcher 보조 · fixer 보조 · final DA가 조용히 실행되지 않았다**. CHANGELOG의 "호출자 8곳 통일"은 **거짓이었다**.
- **`#N8` 목차 앵커 해소**: 본 릴리스가 24개 모듈에 추가한 목차의 앵커가 실제 heading과 불일치(8곳). ⛔ **그걸 고치려 쓴 첫 스크립트가 `\s+ → -`로 공백 연속을 합쳐 14곳을 새로 깨뜨렸다** — GitHub slugger는 구두점 제거 후 **공백 하나당 하이픈 하나**다(`A + B` → `a--b`). 총 **22개 앵커 정정**(대부분 선재 파손), 미해소 **0**.

---

## 2. inert frontmatter 51선언 제거 + Truth-of-Source 정본화

`team-agents`(9) · `composable`(21) · `model-strategy`(21) — 전부 **fz 자작 필드로 런타임 효과 0**. 실효 결정자는 팀 구성·모델 = `workflows/*.js`, 파이프라인 = `provides`/`needs`. `arch-critic: main:opus` ↔ `code-auditor: main:sonnet` 형제 불일치가 stale 위험을 실증했다.

- 소비처 8곳 동반 갱신 + `governance.md §Truth-of-Source`에 **4항목 정본 지정**(모델 배정 · 팀 구성 · opus 상한 · YAML 필수 필드).
- YAML 필수 필드를 **2층 분리**: L1 Claude Code 공식 4 / L2 fz 정책 2(`provides`·`needs` — `/fz` §3.2가 실제 소비). 3판본 충돌 해소.
- opus 동시 상한 **≤3 통일**(정본 `fable-model-guide.md` §5) — `skill-authoring.md` §12만 ≤2로 이탈했고 `plan-collaborative.js`는 Stage 2에서 opus 3 병렬이라 **참조 구현이 규약을 위반**하고 있었다.
- **Agent-Payload 스킬 범주 신설**: `user-invocable: false` + `skills:` 사전주입 스킬(arch-critic · code-auditor)이 어느 범주에도 없어 6게이트를 영구 미충족한 것이 2026-06-28 지적이 1년 미해결된 근본 원인. ⛔ 면제가 아니라 **대체 게이트 3**.

---

## 3. `modules/cross-validation.md` §Negative-Result Gate 신설

⛔ **신규 규칙이 아니다.** `system-reminders.md` T8이 *"정규식 불완전은 Coverage Gate 담당"* 으로 이미 위임했으나 **수신처에 구현이 없었다**. Coverage Gate가 *범위*(N/M)를, 본 Gate가 *도구 유효성*을 본다.

3요소: **positive control**(0건 결론 전 동일 명령이 반드시 매칭되는 케이스에서 non-zero 확인) · **신호 보존**(`>/dev/null 2>&1` 금지 + exit code 판정 포함) · **귀속 라벨**(다중 대상 스캔에 식별자).

근거: 단일 세션 **12 인스턴스** 실측 — 그중 *0건 자체를 의심해서* 잡은 건 **0건**(전부 외부 지적 · 우연한 재측정 · 도구 에러메시지).

- ⛔ **선재 버그 실증**: `fz-peer-review/references/test-spec.md:80`의 `grep -E '^\s*(import\|from)'`은 ERE에서 alternation이 아니라 **항상 0건** → 그 stdlib 검사가 지금까지 통과로 오인돼 왔다(잘못된 패턴 0건 / 올바른 패턴 3건).
- 기계 검출 3항목: **N4** ERE alternation 오용(⚠️ BRE의 `\|`는 정당 → `grep -E` 동일 줄 한정) · **N5** 신호 폐기 · **N6** 루트 앵커.

---

## 4. 실패 복구 사다리 L1~L4 정본화 — OQ3 해소

⛔ **문제**: 5개 스킬이 `mode:'fallback'` 절차로 `modules/team-core.md`+`patterns/`(679줄)를 지목했으나 내용은 `TeamCreate`·`SendMessage` P2P였다 — 이 도구들은 v2.1.178부터 **부재**하고 SOLO에는 에이전트도 없다. **가장 필요한 순간의 지침이 실행 불가**였다.

⭐ **실측이 처방을 바꿨다**: 워크플로 실패는 **2회 실제 발생**했고(`§5.7` fz-code #1 args 오류 · fz-review #8 초회 7287s 스톨) **두 번 다 재invoke·resume으로 복구**됐다. `team-core` 사용 이력은 **0건**. → 679줄 재작성이 아니라 **이미 작동한 경로를 성문화**하는 작업이었다.

- `guides/skill-authoring.md` §12: **L1** 분할 재invoke(H5) · **L2** 입력 오류 → 수정 재invoke(⛔ 워크플로 실패가 아니라 **설계된 fail-fast**) · **L3** 스톨·일시장애 → **`resume` 우선**(동일 세션 한정) · **L4** 사용자 에스컬레이션(⛔ Lead 단독 SOLO는 승인 후에만). 각 단계에 실측 선례 명기.
- `team-core.md` **강등**("실행 절차로 참조하지 않는다") — 679줄은 **설계 출처**로 존치, 재작성 0줄.
- `governance.md` Kill-Switch = **사다리 L4에서 '중단' 선택 시의 절차**로 위치 확정. 부재 도구(`TeamDelete`·`shutdown_request`) 지시 제거 — **비상 경로가 v2.1.178부터 없는 도구에 의존**하던 상태였다.
- **`agents/impl-correctness.md` `tools:`에서 쓰기 7종 제거**(`Edit`·`Write`·`Bash`·`replace_symbol_body`·`insert_*`·`rename_symbol`) — 유일 소비자 `code-pair.js`가 changeset JSON만 요구하고 Lead가 적용하므로 vestigial이었다. 이전에는 **프롬프트 금지만이 방어**였고 capability는 남아 있었다 → 이제 **시도 자체가 불가**(`harness-engineering` "스키마 수준 필터링" + "capability ≠ authorization").
- **동반 수정**: `templates/agent-template.md`에 `Code agent | … Edit, Write, Bash | impl-correctness` 행이 남아 **본 템플릿으로 에이전트를 만들면 capability 축소가 되살아났다** → `Changeset producer`로 정정.

### 4-a. 전수 재조사 — 폴백 지목 잔존 11곳

⛔ 위 적용 후 "폴백을 team-core/patterns로 지목: 0건"이라 보고했으나 **편집한 5곳만 검사한 결과**였다(Coverage Gate 위반). `team-core`/`patterns/` 지목 **111줄을 100% 분류**(사료 32 / 정당 17 / 잔존 62) → 실행·폴백 절차 지목 **11곳 정정**. 스킬 7곳 + 부재 도구 처방 4곳, 그중 **`templates/skill-template.md:115`** 는 생성 템플릿이라 미래 스킬에 전파되던 것이다.

---

## 5. codex 호출 정본 경로 + 출력 계약 검증

⛔ **실측 실패 2건이 신설을 유발했다**: ① `codex exec review --uncommitted "<prompt>"` → **exit 2**(`subcommands-core.md:36`이 "인자 충돌"을 **이미 문서화**하고 있었다 — 산문 경고는 호출 시점에 읽혀야 작동한다) ② 래퍼 마지막 문장(`wc || echo`)의 exit이 태스크 exit으로 올라가 **codex exit 2를 0으로 보고**했다.

- **`scripts/codex-exec.sh`** (`fz-codex-bash-hygiene.md` §8 정본): 사전 게이트 — 플래그 상호 배타(`review`+PROMPT → exit 10) · 필수 인자 · 경로 실재 · trust_level 경고 · git repo 판정. 사후 게이트 — exit≠0 → **12** / 빈 출력 → **13** / JSON 파싱 실패 → **14**.
- ⛔ **10~14는 전부 측정 실패**이며 "이슈 0건"이 아니다. 통과 시 `GATE-PASS json_ok issues=N verdict=V`를 stdout에 명시.
- 검증: 사전 게이트 **8종 전부 발화**(exit 10×6 / 11×2) + 성공 경로 `GATE-PASS` exit 0. §6 복붙 템플릿은 **참조로 강등**(붙이지 않으면 작동하지 않고, 실측상 누락이 재발했다).
- **`FZ_PLUGIN_ROOT` 초기화 절차 신설**: 소비 10곳 / **할당 0곳** — 8호출부가 전부 빈 값을 넘겨 **Tier 2b가 항상 건너뛰어졌다.** 스킬 base directory 유도 + **마커 디렉토리 fail-closed 검증**. ✅ Tier 2a 심볼릭이 있으면 무증상이라 실측 없이는 드러나지 않았다.
- **`get_codex_skill()` → `get_codex_skill_path()`**: 이전 함수는 Tier 2b 확인 후 **이름만** 반환했는데 호출자 8곳은 항상 `cat ~/.codex/skills/${NAME}/SKILL.md`를 읽었다 → 심볼릭 부재 시 **Tier 3 폴백으로 가지 않고 존재하지 않는 경로를 읽었다**. ⛔ `setup-codex-skills.sh`는 dead가 아니라 **load-bearing**임이 확정됐다.

---

## 6. 리뷰 스킬 구조 판정 축 신설 (초안 4.24.0)

리뷰가 결함(요구사항·버그·영향범위)은 잘 찾고 **더 나은 구조는 못 찾는** 문제를 배선으로 해결한다.

⭐ 근거는 **통제 A/B 1건**: 스키마·cap·에이전트·모델을 전부 고정하고 브리프만 결함축→구조축으로 바꿨더니 `review-arch` 1콜(116K)이 대안 **9/10** · 삭제라인 정량 **10/10** · 기존 3-렌즈 24건(533K) 미포착 **신규 6건** · 삭제가능 **95줄(변경량 29%)** 을 냈다. 능력 부족이 아니라 **하네스가 묻지 않았다**.

- **`modules/review-structural-axes.md` 신설** — 5축(메서드 책임 밀도 / 구조 대안 ≥2 / 삭제>추가 / 구조결정 3축 / 코드 형태). **fz-peer-review ↔ fz-review 공유.** ⛔ **경계 문구가 필수 구성요소**("버그는 다른 리뷰어 담당 — 너는 더 나은 구조만 본다").
- **`structuralContext` args 신설** — ⛔ **arch 렌즈에만 주입.** quality/correctness가 결함 축을 유지해야 하는 회귀 방어 + A/B가 `review-arch` 1개로만 측정된 검증 범위 일치. 비용 Tier 2 총량의 **약 0.2%**.
- ⭐ **`dist`가 `suggestion`을 집계하지 않던 결함 수정**: `severity` enum에 `suggestion`이 **있는데** distribution·log에서 누락됐다 → **구조 개선 제안이 정확히 그 등급**이므로 개선 조치가 집계에서 사라지는 구조였다.
- **`improvement → severity cap: minor` 삭제 3곳**: 구조 개선은 정의상 `improvement`인데 cap이 minor로 깎아 "major 이상에만 `alternatives` 필수" 의무를 **구조적으로 무력화**했다. 실측 부작용: severity를 얻으려 `regression` 태깅 → 3 표본 38건 중 `improvement` **1건**.
- ⭐⭐ **자기 리뷰 반영 (같은 릴리즈)**: 구현 후 `/fz-review` verdict `needs_revision` → 9건 수정. **Codex 이종 검증이 major 5건을 단독 발견** — 자기 리뷰만으로는 major 7건 중 4건을 놓쳤다. 그중 **정본 호출 미배선**(Tier 2/3 정본 시퀀스 args에 `structuralContext` 부재 → 에러 0·로그 0·구조 축 0으로 정상 반환)과 **모드 커버리지 0**(Tier 0 `<100줄`이 실무 최다 규모인데 도달 경로 없음)이 핵심이었다.
- ⛔ **트리밍 비저하 위반 1건 (자기 위반)**: 삭제 34줄 전수 분류 결과 투표 로직의 `→ "3/3 동의" 시 Fact Verification Gate 재확인 권장.` 1줄이 **대체 없이 순삭**됐다. 진단은 남고 조치만 사라진 상태였고, 지운 동기는 **500줄 아래 자리 확보**였다 → 병합 복원(줄 수 증가 0).

---

## 7. `llm-references.md` §1.1b 신설 + advisor 3중 사각지대 (초안 4.23.0)

감사 결과 **fz가 실행 경로에서 의존하는 Claude Code 기능 7종의 공식문서 행이 §1.1에 없었다.** 특히 `/model-config`은 **12개 지점에서 이미 1차 출처로 인용**되면서 색인 행만 없어 참조점이 분산돼 있었다. 7개 전부 **원문 fetch 후 작성**.

⭐ **fz 규약에 직접 영향 2건**:
- `CLAUDE_CODE_SUBAGENT_MODEL`이 subagent·agent team·**workflow agent** 전부에 적용되고 **per-invocation `model` 파라미터와 frontmatter `model`을 override**한다 → `skill-authoring §12`의 "model 명시 의무"를 무력화할 수 있는 유일한 변수.
- `/env-vars`: *"any non-empty value **including `0`** turns the behavior on"* — **`=0`은 끄는 값이 아니다.**

⛔ **stale 정정 — 세션 생애 200 스폰 캡 제거**(v2.1.224) 5지점. 남은 하네스 상한은 **동시 20 + depth 3**뿐. fz governance(opus 동시 ≤3)는 여전히 훨씬 보수적이라 **거버넌스 재설계 불필요** — 순수 사실 정정이다.

**advisor 실증 2건 → `governance.md` §사각지대 신설**:
- ⭐ **Workflow `agent()`가 세션 `advisorModel`을 상속한다** [verified: 1-agent 프로브].
- ⭐ **advisor 사용량은 트랜스크립트에 기록되지 않는다** [verified: 트랜스크립트 3.1MB 전수 — `usage.server_tool_use` 289 레코드 전수가 web_fetch/web_search만].
- ⇒ advisor가 ① kill-switch ② lint/런타임 캡 ③ 트랜스크립트 계측 **3중으로 안 잡힌다**. `/usage`(대화형)가 유일 관측 경로. ⛔ **`opus 동시 ≤3` envelope이 advisor 지출을 bound하지 않는다.**

---

## 8. 4라운드 자기 감사 → `NOT CONVERGING` → 감산 전환

구현 diff를 `/fz-codex`로 4회 교차검증했다. R1=15(critical 3) → R2=15(critical 3, **11건이 R1 수정에서 발생**) → plan 검증=11 → R3=14(critical 1) → R4=10(critical 0). R4에서 외부 판정이 **NOT CONVERGING** 이었다 [외부: codex]:

> "The structural cause is **reactive expansion of bespoke partial interpreters**, followed by **self-tests derived from the same assumptions**. Each counterexample adds another regex, merge rule, waiver, or fixture **without an independent semantic oracle**."

⛔ **그래서 패치 루프를 멈추고 감산했다.**

- **`#15` 삭제** (외부 C4 권고 수용, **−112줄**): 후보 15건에 현재 위반 0 · 역사적 결함 2건은 이미 구조적으로 수정됨 · 항목 설명이 스스로 오탐을 인정 · R3/R4에서 문맥창·waiver 3종·heading 교차를 얹고도 H1·setext 경계를 놓쳤다. 부수: R4-ISSUE-007이 **삭제와 함께 소멸**.
- **`validate-codex-output.py` 전면 재작성 — 범용 JSON Schema 자작 폐기**: ⭐ **실측이 방향을 뒤집었다** — `schemas/*.json` 전수에서 **`$ref` 0건** · 반면 `pattern` **7건**. 4라운드에 걸쳐 `$ref` 해소·JSON Pointer·형제 병합(**가장 어려운 부분**)을 자작했고 그것은 **존재하지 않는 요구사항**이었다. 지원 키워드를 **실측 빈도로 재정의**하고 **미지 키워드는 exit 2**(침묵 금지). ⛔ 범용 검증기가 아님을 docstring에 선언한다.
- **`#N6`를 줄 화이트리스트로 — `ast` 일반 분석 폐기**: R4 실측에서 `from evil import Path as P` → `P(__file__)` 통과 · `X().resolve(__file__)` 통과. 대상은 우리 스크립트 **10개**뿐이므로 실제 쓰는 줄을 열거하고 **미지 형태는 거부**. heredoc은 POSIX 종료 규칙 재구현을 폐기하고 **첫 `<<` 이후 전체를 데이터로 취급**(fail-closed) → R4-ISSUE-006 3방향 동시 소멸.
- ⛔ **Lead 자체 발견**: 전환 중 내가 넣은 범용 패턴 `[A-Z_]*="$(cd "…" && pwd)"` 가 **경로 무관**하게 허용돼 `cd "/tmp"`·`$HOME`도 앵커로 인정했다 — **fail-closed 화이트리스트에 내가 구멍을 냈다**. 자기 위치 유도 경로로 제한.
- **정정 4건**: `codex_review_schema.json` `confidence` 경계 인라인(`confidence: 101`이 통과했다) · `health-check.sh` 3상태 표기 + **UNRUN을 FAILED보다 먼저 판정** · `PARAGRAPH_LINE` 축소(마커는 뒤에 공백이 있을 때만) · 위치 단정을 `startswith` → 경계 요구(`:3`이 `:30`에 매칭됐다).

**결과 — 순감 `−132줄`** (`lint_contracts.py` 1341→1229 · `validate-codex-output.py` 188→168). ⛔ **`plan-final.md` §변경 예산 초과(+418줄 · 스크립트 3개)를 실제로 되돌렸다** — 외부 검증이 그 초과를 독립 확인했고(+511/−94 net +417), 감산이 그 지적의 처방이었다.

---

## ⛔ 미해소 / 사각지대 (출하 시점 명시)

- **5라운드 검증 미실행** — codex 한도 소진으로 실행 불가. 외부가 제시한 반증 기준(critical 0 · major ≤2 · 직전 수정에서 발생한 결함 0 · mutation/differential 테스트)은 **미판정**이며, 감산 이후 표면(validator 전면 재작성 · `ANCHOR_LINES` · `health-check` 재구성)은 **외부 리뷰를 받지 않았다**. 한도 회복 후 재실행 권장.
- **기계 검사 사각지대 (candidate)** — CHANGELOG의 **레지스트리 항목 카운트**는 어떤 lint도 보지 않는다(`#N2`는 디렉토리 *파일 수*만). 이번 사이클에 두 번 stale했다. 항목 추가·삭제 시 `--list` 수동 전사 의무. ⚠️ 검사 신설은 **검증 라운드 직전에 새 표면을 만들지 않기 위해** 보류했다.
- **`#1` 필수 필드 값 검증** — 키 *존재*만 보므로 빈 값이 통과한다. 21개 스킬 전부 필수 키를 유지해 **현재 결함 0**이고 YAML 파서 도입은 비례적이지 않다. 계약 불완전성으로 기록만.
- **OQ5 `description` 400자 cap → 수정 불필요** (실측 판정): `schemas/*.json`·`workflows/*.js`에 `maxLength` **0건**. `peer-review.js:41`의 `≤400chars`는 필드 설명 문자열 안의 **권고**일 뿐이고 초과 시 에러·잘림·거부가 없다. 리스크로 등록했던 것이 **오판**이었다.
- **OQ9 리뷰 이슈 3중 계약 → 위생, 미처리**: 지시 없던 required 4필드가 `scope_disposition` 13/13 · `code_snippet` 13/13 · `alternatives` **8/13 substantive**, 잔여 5는 스키마가 `type:["array","null"]`이라 정당. 실제 피해 0 → 계약 수렴은 "지금 잘 되는 것을 건드림".
- **후속 분리 티켓**: `evidence_trace` 개명 10곳 · `CounterSchema.verdict` 2종→4종 · `fz-codex-subcommands-core.md:156-158` verdict 계약에 `suggestion` 미정의(소비자가 `fz-fix`라 리뷰 스킬 범위 밖 — Surgical Changes).

---

## 버전 번호 정리 노트

| 항목 | 정리 전 | 정리 후 |
|------|--------|--------|
| CHANGELOG 섹션 | `[4.25.0]`·`[4.24.0]`·`[4.23.0]` ×3 | `[4.23.0]` ×1 (본문은 §A·§B·§C로 전량 보존) |
| `plugin.json` / `marketplace.json` | 4.25.0 | 4.23.0 |
| 태그 / GitHub Release | 없음 | `v4.23.0` |

폐기된 초안 번호 4.24.0·4.25.0은 **발행된 적이 없으므로 재사용 가능**하다. 다음 릴리즈는 4.24.0부터 이어진다.

⛔ **이 정리는 v4.22.0에 이어 두 번째다** — [v4.22.0 §버전 번호 정리 노트](v4.22.0.md)가 같은 이유로 4.23.0~4.25.0을 폐기했고, 그 번호들이 **다시 초안으로 소비됐다**. 반복 원인은 릴리즈 절차의 누락이 아니라 **커밋 시점에 매니페스트를 먼저 bump하는 관행**이다: bump가 "발행 예약"으로 읽히지만, 태그와 Release가 없으면 그 번호는 어느 소비자에게도 존재하지 않는다. 매니페스트 bump를 **발행 커밋에서만** 하는 것이 구조적 해법이고, 그때까지는 이 정리가 다시 필요할 수 있다.
