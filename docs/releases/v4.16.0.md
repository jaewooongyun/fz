# v4.16.0 (2026-06-27) — SKILL.md 분리 + visual oracle 강화 [MINOR]

## 배경

TVG-1219(유튜브식 플레이어 제스처) 작업의 `enhancement-strategy.md`에 적힌 실수·문제·못 찾은 점을 fz에 환류한 세션. 핵심 발견은 **fz가 자기 자신을 고도화할 때의 구조적 한계** — 동종(Claude-only) self-review가 못 잡는 blind spot이 반복됐고, 모두 `/fz-manage check`(전제 검사) 또는 사용자 catch로 드러났다.

3겹 메타 교훈 (`feedback_fz_self_reference_blindspot` 3발현): (1) 활용 갭 — Workflow 보유하나 raw Agent 우회, (2) 자기참조 편향 — fz 평가 시 책임 최소화, (3) 작업 전제 미검증 — 구현은 N중 검증하나 "작업이 필요/정당한가"는 안 물음.

## 변경 (adopt-now)

### 1. fz-review visual oracle 완료기준 강화 (검증5/Gate)
- UI/제스처/애니메이션 *동작* 변경 시 **빌드 통과 ≠ 완료** — visual oracle(launch_app_sim+screenshot / 실기기 시각 확인) 미충족 시 "완료" 선언 지양.
- fz는 런타임 동작을 정적으로 못 봄(자동 실행 불가 = 사람 영역, 🔒 강제 게이트 불가) 명시.
- evidence 3 sessions(38차 ASD-1398 · user_spec ASD-1793 · TVG-1219) 기반 기존 'screenshot 권고' 강화. Gate 4 항목 동기화.

### 2. SKILL.md 500↓ 분리 (Progressive Disclosure)
- **fz-review 560→436**: 조건부 검증 4-D/4-E/4-F/4-G/4-H + candidate 4-N/4-O 본문을 `modules/review-checks.md`(신규)로 MOVE(본문 1:1, 트리밍 비저하 준수). 항상 실행 검증 1~3·Gate 4·검증5는 SKILL.md 잔존. 4-I/4-J/4-K는 이미 다른 modules 참조라 포인터 유지.
- **fz-peer-review 523→463**: Auto-Tier 판정 bash를 `modules/peer-review-tiers.md`(기존, "자동 휴리스틱 SSOT" 선언)로 **일원화**. 단순 분리가 아니라 중복 제거 — tiers.md L18 "SKILL.md는 요약만" 규칙을 SKILL.md bash가 위반하고 있었음. 표(CHANGED_LINES 개념)↔bash(SIGNIFICANT_LINES 판정) 변수 불일치도 명확화("판정은 bash 신뢰").
- 전체 SKILL.md ≤500 달성 (이전 2건 초과 → 0건).

### 3. candidate 2종 (evidence 1 session — 5 sessions 후 active 재판정)
- **P1 Sibling-Convention Check** (`skill-authoring.md`): fz 자산에 candidate/검증/관측 추가 시 기존 동류 항목(4-N/4-O·L-series) 표기 템플릿 grep 일치.
- **L-5 대칭/짝 경로** (`promotion-ledger.md`): expand↔collapse 등 상보적 쌍 한쪽만 수정 누락 관측.

### 4. fz-memory L1 audit 강화
- entry 200자 초과 라인 검사 + MEMORY.md↔topic 양방향 정합(orphan 탐지) + organize 제안 (auto-force X — 40차).

## Reverted

- **C(§1 → skill-lifecycle.md 분리) + M4(헤더 정규화)**: skill-authoring §1을 modules로 분리했으나, `feedback_guide_line_limit`(guides/는 500 면제 + 줄 수 위한 분리 금지)를 위반. `/fz-manage check`가 전제 오류를 catch → `git show HEAD` 원본 복원으로 revert(P1만 보존). harness-engineering 1155줄도 단일 유지하는 확립된 패턴 정합. "구현 검증 ≠ 전제 검증" 교훈(발현3).

## 영향 갱신 (③ 짝경로)

- 검증 4-E 인용 2곳(`agents/review-quality.md` · `skills/fz-manage/prompts/generate_suggestion.md`)을 review-checks.md로 갱신. fz-review §4-* 포인터·Few-shot 예시에 review-checks.md 힌트 추가.

## 가이드 준수

- **트리밍 비저하** [prompt-optimization §1 보충 3a]: 분리는 MOVE(본문 1:1), 요약 아님. review-checks 체크리스트 14건 보존, bash 1:1.
- **SSOT**: peer-review-tiers L18 "SKILL.md 요약만" 준수 (bash 일원화).
- **1단계 깊이** [skill-authoring §3]: SKILL.md → module 1단계.
- **DELETE/MERGE-default**: 분리=MOVE(additive 아님). guides/ 분리 금지(skill-authoring revert로 단일 복귀).

## 검증

- ⛔ Codex cross-model verify 미수행 (quota ~2026-06-30 리셋). **동종 Workflow 3-lens(가이드준수/본문1:1/영향·counter) + Lead 실측** 대체. Workflow가 낸 major 5건 중 Lead 실측으로 **false positive 2건 차단**(M1 출처주석을 로딩참조로 오독·M3 역사문서를 현재로 오독), 경미 3건 갱신, 1건 pre-existing(F4). 23차(self-review blind spot)의 정량 실증 — counter 미검증 시 Lead 실측이 유일 안전망. 회복 시 후행 check.
- `/fz-manage check`: 전체 SKILL.md ≤500 / 포인터 목적지 실재 / §4-* stale 인용 0 / SSOT boundary 표↔bash 일치 / 금지패턴 0.
- 산출물: `~/dev/TVING/fz-tvg1219-feedback/` (plan-v4~v6, follow-up F1~F4).

## Deferred (follow-up)

- **F1**: "DELETE/MERGE-default §3" 오인 4곳 (README·skill-authoring §12 현재 문서 정정).
- **F2**: 트리밍 비저하 원칙 중복 (skill-authoring §3 ↔ prompt-optimization §1 보충 3a).
- **F4**: peer-review-tiers bash에 `--codex` 분기 부재 (pre-existing, SSOT 일원화로 표면화).
