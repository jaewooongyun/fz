# Release v4.7.1 (2026-05-03)

**Title**: Implicit→Explicit Enforce: 11 actual fix UC + 5 verified-clean (3-Phase 통합)
**Type**: MINOR
**Cost note**: UC-6 Lead Spawn Override 의무화로 Primary Worker ~2x cost increase 예상. 1주 baseline 모니터링 의무 (`experiment-log §5.5 cost_proxy`).

## TL;DR

v4.6.0 deep analysis로 발견된 24 update candidates를 4-Phase로 분할 처리 → 11 actual fix UC + 5 verified-clean을 단일 minor release로 통합. **핵심 가치: 가이드 본문(docs)에 implicit하게 있던 규칙을 explicit reference + verification command로 enforce.**

## Highlights

### 1. Trimming 비저하 Single Source 확립 (UC-7)

`prompt-optimization §1 보충 3a` = 단일 source. 다른 가이드(skill-authoring §3, plan-deep-planning, fz-plan/SKILL)는 reference link로 통일. drift 위험 0.

### 2. 4 SKILL Lead Spawn Override 명시 (UC-6)

`team-registry L10-12` "Lead가 model='opus' 명시 전달" governance가 docs only였던 문제 해결.
- fz-plan: Round 0.5 → Round 1 Sequential Operating Contract (lifecycle: `shutdown_request → 종료 확인 → TeamDelete`)
- fz-code/review/fix: `Agent(name="...", model="opus")` 명시

frontmatter mutation은 회피 (Codex Phase 3 v1 Critical로 catch). governance "동시 opus ≤ 2" 보장.

### 3. NLAH 6요소 ↔ H1-H6 매핑 (UC-4)

harness §1.2 NLAH 형식 정의 + prompt-opt §H1-H6 design principles이 두 가이드에 분리되어 있었던 문제 해결.

`harness §1.2.1 NLAH ↔ H1-H6 매핑`:
- C(Contracts) ↔ H1(가정 인코딩) + H2(외부 evaluator)
- R(Roles) ↔ H2(Generator≠Evaluator) + H5(Initializer/Coding split)
- S(Stage Structure) ↔ H5 + H6(loop primitives)
- A(Adapters) ↔ H1
- Σ(State Semantics) ↔ H3(Context reset) + H4(session 외부 로그)
- F(Failure Taxonomy) ↔ H6 **partial** (escalation/rollback/abort은 NLAH-specific)

`[verified: harness L33-L38 NLAH + prompt-opt L598-L673 H1-H6]`

### 4. Pattern 모듈 명시 reference (UC-11)

dependency-graph "거짓 dangling" 정정. 9 SKILL이 합성어 reference (`team-core + patterns/`)만 사용했던 문제 해결.

| Skill | Specific Pattern |
|-------|-----------------|
| fz-discover | patterns/adversarial.md |
| fz-plan | patterns/collaborative.md |
| fz-code | patterns/pair-programming.md |
| fz-review | patterns/live-review.md |
| fz-peer-review | patterns/live-review.md (공유) |
| fz-search | patterns/cross-verify.md |

### 5. Same-model Cross-Verify 정책 (UC-5)

skill-troubleshooting L214 "동종 모델 맹점" single source 확립.
- **Canonical (headline_rate_pct)**: 동종 모델 cross-verify는 Reflection Rate에서 **exclusion**
- **Auxiliary (weighted_rate_pct)**: 가중치 0.5 적용 별도 보고

experiment-log §5.5 schema 두 metric 분리 (legacy `rate_pct` 호환 유지).

### 6. lesson-intake → memory-guide 통합 (UC-10)

17줄 fragment 모듈 (참조 1건) → `memory-guide § Lesson Intake Decision Tree` 흡수. modules 26→25.

Progressive Disclosure 정합 (16줄 fragment 격리 해소).

### 7. UC-9 catch_rate threshold + UC-13 harness ## Index

- **UC-9**: `swift-anti-pattern-preblock § Catch Rate Threshold` 신설 (>30% 강화 / <5% 비활성화). experiment-log §5.6 P1/P2/P3 컬럼
- **UC-13**: `harness ## Index` 신설 (heading-based anchor only, 15줄 ≤30 budget)

### 8. UC-12 evidence-collection ↔ peer-review-gates 방향 명시

acyclic 보장. 합병 회피 (Progressive Disclosure 보호).
- evidence-collection = **Producer**
- peer-review-gates = **Consumer**
- procedure cross-reference (Gate 4.4-A 참조 등)는 dependency edge가 아님

### 9. UC-14 review-counter 항상 실행 + UC-17 Phase B 통합 표

- **UC-14**: fz-review review-counter `[선택]` → `[항상 실행]` (Sycophancy 방어)
- **UC-17**: experiment-log § "Phase B 시작점 정의 (cross-experiment 통합 표)" 신설

## ISSUE 정밀 처리

| ISSUE | 처리 |
|-------|------|
| **ISSUE-016** | UC-6 documented operating contract (runtime guarantee 주장 제거) |
| **ISSUE-017** | sprint-contract.md 6 위치 amendments — N≥5 → N≥10 통일, auto-trigger 표현 제거 |
| **ISSUE-020** | AC-9 portable awk script (per-hunk + same/adjacent line + governance 키워드 narrow) |
| **UC-6 cost_blast** | experiment-log §5.5 `cost_proxy` schema deterministic formula |

## 메타 패턴 (F1/F3/F4)

본 release 자체에서 fz의 메타 안전망 실전 검증.

- **F1 entity 누락**: 1차 분석에서 fz-modernize (22번째 스킬) 누락 → Codex 2차 verify catch
- **F3 state 누락**: UC-16/18/20 already-addressed UC catch → Phase 2/3 probe-first 효과 검증
- **F4 외부 정의 추정**: UC-4 NLAH 매핑 추정 → Codex Phase 3 v1 Critical 1건 → v2 verified evidence 첨부

## STC-1 (Operational Stop Condition) 정상 작동

```
Phase 1 plan: v1 → v2 (66.7%) → v3 (16.7% regression) → STC-1 발화 → 분할 결정
Phase 2 plan: 즉시 시작 (한도 0/3 보존)
Phase 3 plan: v1 (Critical 1) → v2 (77.8%, Critical 0) → Lead inline fix (한도 1/3 보존)
```

18차 Scope Inflation + STC-1 + Verification Discipline 모두 작동. 사용자 결정 의무 3회.

## Verified-clean (5 UC, probe로 already-addressed 확인)

| UC | 비고 |
|----|------|
| UC-8 | swift-pattern-detection D/E/F/G 이미 v4.6 정의됨 |
| UC-15 | fz-new-file 호출 위치 fz-code SKILL L191 1건 (정책 유지) |
| UC-16 | "3-Model Triad" stale ref 0건 (v4.4 이미 정리) |
| UC-18 | experiment-log §5.5 L175/L181 이미 명시 |
| UC-20 | fz-commit/fz-pr `## 규칙 참조` 이미 명시 |

## Phase 4 deferred (op-ticket workflow)

7 UC (UC-1/2/3/21~24)는 데이터 임계 도달 시 Lead checklist step (T2-B Sprint Contract 진행 시 manual review). "자동 trigger" 표현 완전 제거.

## Cost Impact (UC-6)

Primary Worker spawn 시 model="opus" 명시 override → ~2x 비용 증가 예상.

**1주 baseline 측정 의무** (`experiment-log §5.5 cost_proxy`):
- `opus_turns = sum(agents[].turns where model == "opus")`
- `opus_minutes = sum(completed - created where model == "opus")`
- `total_agent_turns / total_agent_minutes`

머지 전 1주 평균 vs 머지 후 1주 평균 → cost increase ratio 산출.

## 파일 변경 통계

```
21 modified + 1 deleted (lesson-intake.md fragment)
+281 insertions / -57 deletions
```

## 가이드 root cause 매핑

| 변경 | 가이드 root cause |
|------|-----------------|
| UC-7 | prompt-optimization §1 보충 3a 본문 (drift 위험) |
| UC-10 | skill-authoring §3 Progressive Disclosure (16줄 fragment 부적절) |
| UC-9 | harness §7 Ablation (Load-bearing/Neutral/Overhead 분류) |
| UC-11 | prompt-optimization §1 1단계 깊이 reference |
| UC-12 | prompt-optimization §3 Progressive Disclosure (acyclic) |
| UC-14 | skill-troubleshooting §3.4b 동종 모델 맹점 |
| UC-4 | harness §1.2 NLAH + prompt-opt §H1-H6 (연결 부재) |
| UC-5 | skill-troubleshooting L214 single source |
| UC-6 | team-registry L10-12 + agent-team-guide L93/L282 (docs only) |
| UC-13 | prompt-optimization §3 Progressive Disclosure (1089줄) |
| UC-17 | harness §7 Ablation (실험별 N 값 single source) |

## 다음 단계

1. **UC-6 cost_blast 1주 baseline 측정** (의무)
2. **F1/F3/F4 메타 교훈 메모리 저장** (사용자 결정)
3. **Phase 4 deferred 7 UC** — op-ticket workflow (T2-B Sprint Contract 진행 시 Lead manual review)

## Credits

- Codex CLI 0.124.0 (gpt-5.5) cross-model verify (2 cycle, plan-final-phase3 verify)
- Verification Discipline (Anthropic 공식 + fz 메모리)
- harness-engineering NLAH 인용 (arxiv 2603.25723)
