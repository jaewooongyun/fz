# Release v4.10.0 (2026-05-27)

**Title**: Sycophancy 방어 + Active Recall + Reflection Pipeline Active
**Type**: MINOR
**Trigger**: 사용자 self-diagnosis 문서 (`fz-meta-improvement-2026-05-26.md`) — ASD-1137 PR 작업 중 누적된 22개 사용자 catch (Sycophancy 동의 편향 + Over-engineering 패턴) 분석 → 6 Priority 개선 도출.
**Cost note**: Codex review 1회 (fz-review 세션 — 3 blind spot 단독 발견). 메모리 18차 한도(연속 3회) 내.

## TL;DR

ASD-1137 PR 작업에서 누적된 22개 사용자 catch를 self-diagnosis하여 6 Priority로 개선. **Active Recall 강제화**(메모리 능동 회상 미실행 차단) + **Sycophancy 방어 4원칙** + **Reflection Pipeline Active**(교훈→모듈 자동 매칭) + **Phase 4 Default 역전**(verify-forever 방어) + **fz-codex 모듈 분리**(757→268줄, skill-authoring 500줄 한도 준수) + **fz-doc·fz-excalidraw 제거**. 8 commits (+ 이전 세션 README 정리 2). 28 files / +1032/-2289 LOC.

## Highlights

### 1. P1 — Active Recall 강제화

기존 Phase 0 Step 4 "교훈 사전 로드"는 *(선택)* 이었음 → **강제화**. 메모리에 교훈이 있어도 active recall 없으면 무효 (메모리 31차).

```
skills/fz/SKILL.md Phase 0 Step 4 — 교훈 사전 로드 (선택)→(강제)
  + Active Recall 3-step chain (관련 메모리 매핑 → 적용 명시 → Gate 확인)
Gate 0 — 메모리 능동 회상 체크 추가 (미회상 시 진행 차단)
modules/lead-action-default.md — 40차 신호 확장("확인해줘/맞아?") + 40차-MUST3 Codex precedence
```

### 2. P2 — Sycophancy 방어 4원칙

무비판 동의 편향을 구조적으로 차단. (적용 위치: TVING `CLAUDE.md` — 본 repo 외 + fz 전반)

```
① 동의 default 금지: "맞다"/"정확하다" 전 근거 실측 (Read/Grep/Bash 1회+)
② 칭찬 절제: "통찰력/정확합니다/완벽" 류 세션당 3회 이하
③ 정정 의무: 사용자 가설 부분 정확 시 부정확 부분 명시
④ 자기 강화 금지: "재확인/정당성 재확인/완전 일치" 류 결론 default 금지
```

### 3. P3 — Base Verification Gate

git diff 분석 전 base 상태를 검증하여 "변경 안 한 코드를 변경한 것으로 오판"하는 패턴 차단.

```
modules/fz-codex-bash-hygiene.md §5.5 — Base Verification Gate
  (git diff 분석 시 base 브랜치/커밋 상태 명시 검증)
```

### 4. P4 — Phase 4 Default 역전

implementation-ready 시점부터 권고 기본값을 **verify → implementation**으로 역전. verify-forever 메타 사이클 방어 (메모리 33차 Recommendation Default Bias).

```
modules/fz-pipeline-proposal.md
  - Default 결정 알고리즘: implementation-ready 판정 시 권고 default = 구현
  - verify 우선은 명시적 안전 우려 시에만
  - 역전 금지 규칙
```

### 5. P5 — Scope Drift + 41차 Reuse-First

확장성 인프라가 명시적으로 존재할 때 신규 작성 default 금지 (메모리 41차).

```
skills/fz-plan/SKILL.md Phase 0.5
  신호: universal/extensible/generic/common 명명 + 5+ 사용처 + URL-agnostic API
  → 신규 RIB/타입 작성보다 기존 인프라 재사용 우선
```

### 6. P6 — Reflection Pipeline Active (Working)

메모리 교훈 → fz 모듈 자동 반영 매칭. Status **Draft → Active(Partial)**. 메모리 17차 (Reflection Gap — 교훈이 기록되지만 모듈 미반영) 도구화.

```
skills/fz-manage/scripts/parse_memory.py
  - 한국어 cross-ref / meta-pattern 패턴 + markdown link 추출 + originSessionId fallback
skills/fz-manage/scripts/score_relevance.py
  - context-anchored 클러스터링 (5a-5d) + cluster name 반환
  - 단어 단일 매칭 → 맥락 결합 매칭으로 false positive 차단
    (19차/41차 fz-modernize 오매칭 회귀 세트로 검증)
docs/design/lessons-to-module-pipeline.md — Status Active(Partial) + §9 통합 결정
```

### 7. fz-codex 모듈 분리 (757→268줄)

`skills/fz-codex/SKILL.md`가 skill-authoring 500줄 한도를 초과 → 3 모듈로 분리.

```
skills/fz-codex/SKILL.md          757 → 268줄
modules/fz-codex-bash-hygiene.md  신규 — §1-6 hygiene + §5.5 Base Verification Gate (185 lines)
modules/fz-codex-subcommands-core.md  신규 — review/verify/validate/check (199 lines)
modules/fz-codex-subcommands-aux.md   신규 — final/commit/adversarial/drift/plan/micro-eval/config (198 lines)
```

> Codex 검증 권고 반영: 3 모듈 목차 추가 + "raw 복붙 금지" warning (bash hygiene wrapper 미적용 시 29/30차 hang 위험) + cross-validation.md·feedback-verification.md stale cross-ref 갱신.

### 8. 스킬 제거 (fz-doc / fz-excalidraw)

```
skills/fz-doc/         제거 → fz-skill write 서브커맨드로 흡수
skills/fz-excalidraw/  제거 (SKILL.md + references 7파일, -1465 LOC)
modules/intent-registry.md  두 스킬 트리거 행 삭제 → fz-skill로 병합
```

### 9. 스킬 품질 가드 (candidate — 5 sessions 관측)

```
fz-code / fz-review 행동원칙 — 자산 추가/수정 시 skill-authoring + memory-guide 명시 참조 의무
fz-review 검증 4-N (Swift Naming Compliance) candidate
fz-review 검증 4-O (Session-added Assets Application) candidate
fz-code Anti-Pattern — Swift Naming 위반 candidate 신호
```

> ASD-1366 기원 (1 session evidence). memory-guide Lesson Intake Decision Tree에 따라 *candidate* 상태 — 활성 강제 아님, 5 sessions 관측 후 활성화 결정.

## Cross-Model 검증

fz-review 세션에서 Codex가 Claude self-review가 놓친 blind spot 3건 단독 발견 (메모리 23차 재실증):

| # | Codex 단독 발견 |
|---|---------------|
| 1 | 분리된 모듈의 bash 예시가 hygiene wrapper 미적용 → 29/30차 hang/sandbox 위험 |
| 2 | "자동 로드" 표현 오기 (모듈은 Level 3 명시 Read이지 자동 로드 아님) |
| 3 | cross-validation.md·feedback-verification.md stale cross-ref (구 fz-codex/SKILL.md 경로) |

→ 3건 모두 반영. self-review가 일부 작동했으나 *최종 안전망은 cross-model*.

## Files Changed

### 변경 (전체 v4.9.0..HEAD: 28 files, +1032/-2289)

**6 Priority 핵심**:
- `skills/fz/SKILL.md` (+12/-) — P1 Phase 0 Step 4 강제 + Gate 0
- `modules/lead-action-default.md` — P1 40차 신호 + MUST3
- `modules/fz-pipeline-proposal.md` (+32) — P4 Phase 4 Default
- `skills/fz-plan/SKILL.md` (+9) — P5 41차 + Phase 2 codex verify
- `skills/fz-manage/scripts/parse_memory.py` (+83) — P6
- `skills/fz-manage/scripts/score_relevance.py` (+108) — P6
- `docs/design/lessons-to-module-pipeline.md` (+46) — P6 Active 전환

**fz-codex 모듈 분리**:
- `skills/fz-codex/SKILL.md` (-495) — 모듈 참조로 축소
- `modules/cross-validation.md`·`feedback-verification.md` — cross-ref 갱신

**스킬 품질 가드**:
- `skills/fz-code/SKILL.md` (+3) · `skills/fz-review/SKILL.md` (+56)

**스킬 제거**:
- `skills/fz-doc/SKILL.md` (-233) · `skills/fz-excalidraw/` 7파일 (-1366)
- `modules/intent-registry.md` · `skills/fz-skill/SKILL.md` (+69, write 흡수)

**릴리즈 + 문서**:
- `README.md` (+/-212) — What's New v4.10.0 + Architecture 개수 정합 (+ 이전 세션 근거연구 표 정리)
- `.claude-plugin/plugin.json`·`marketplace.json` — 4.9.0 → 4.10.0

### 신규 (4)
- `modules/fz-codex-bash-hygiene.md` (185 lines)
- `modules/fz-codex-subcommands-core.md` (199 lines)
- `modules/fz-codex-subcommands-aux.md` (198 lines)
- `docs/releases/v4.10.0.md` (본 파일)

### Untracked (의도적 제외)
- `.fz-work/peer-review-opt-20260512/` — fz 실행 워크 디렉토리 (작업 데이터)
- `experiment-log-traces.jsonl` — Phase 5 실험 trace

**제외 근거**: 메모리 36차 — `.gitignore`도 팀 공유 영역. 자동 수정 없이 커밋 제외만 적용.

## Migration

**Breaking changes**: 없음 (모두 additive).

**스킬 제거 영향**:
- `fz-doc` 사용자 → `/fz-skill write` (문서/프롬프트 최적화 기능 흡수)
- `fz-excalidraw` 사용자 → 외부 다이어그램 도구 (Excalidraw 웹 등)

**fz-codex 모듈 분리 영향**:
- `/fz-codex` 호출 동작 동일 (서브커맨드 정의가 모듈로 이동했을 뿐)
- 모듈은 Progressive Disclosure Level 3 (명시 Read) — 자동 로드 아님

## 다음 릴리즈 예고

- **5-session outcome 측정**: 6 Priority 개선 효과 + candidate 검증 4-N/4-O 활성화 판정 (5 sessions 관측 후)
- **Reflection Pipeline 정밀화**: fz-modernize 5-memory 매칭 precision 추가 검증

## 참조

- self-diagnosis: `~/dev/TVING/fz-meta-improvement-2026-05-26.md`
- 분석 산출물: `~/dev/TVING/fz-meta-2026-05-26/` (final-report / improvement-plan / codex-verdict / review)
- 본 릴리즈 commits: `git log v4.9.0..v4.10.0` (10 commits)
- 메모리 메타 학습: 17차 (Reflection Gap), 18차 (Scope Inflation), 23차 (Cross-model 안전망), 31차 (Plan-before-Probe), 33차 (Recommendation Default Bias), 40차 (단순 요청), 41차 (Reuse-First)
