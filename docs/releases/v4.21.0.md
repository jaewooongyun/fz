# v4.21.0 — 하네스 서베이 Wave C: code-pair pre-flight 크기 가드 (H5) (2026-07-09)

## 배경

plan-final Wave C — 하네스 홀 candidate 7개(H1~H5/F5/F6) 중 **유일 미구현이던 H5** 조치. H5 실사고: ~800줄 changeset을 단일 StructuredOutput(newBody exact syntax)으로 요구하다 재시도 5회 초과 → null → **27분·206K 토큰 소모·디스크 무변경**. 서베이 scaffold collapse(2605.12129) 강한 유사 구조 + retry budget 미설계(2605.21516).

## C1 — 캘리브레이션 (임계 확정)

changeset newBody 크기별 StructuredOutput 성공률을 실제 ChangesetSchema 동형으로 실측(sonnet ×4):

| 목표 | 산출 | 실제 줄수 | 의사코드 |
|:---:|:---:|:---:|:---:|
| 100 | ✅ | 103 | 없음 |
| 200 | ✅ | 196 | 없음 |
| 300 | ✅ | 298 | 없음 |
| 500 | ✅ | 492 | 없음 |

**null 0/4.** ≤500줄 실증 안전(통제된 실패 0건). → **SPLIT_THRESHOLD=600 (provisional)**. ⚠️ **스코프 정직 고지(자체 리뷰 정정)**: plan은 4축 캘리브레이션(줄수 50~800+·파일수·symbolEdit·oldAnchor)을 명세했으나, 실제 C1은 **사용자 승인 하에 소규모 단일축**(줄수·단일파일 4점)으로 축소 실행 — 800 미도달·다파일 조합 미측정. 따라서 600은 **확정이 아닌 보간값**(하한 500 실측 + 상한 800 일화). 계획 추정 150줄이 과도했음은 확인(150이면 over-decomposition). **향후 관측 실패에서 재조정 필수**. 상세·미측정 축: `code/c1-calibration.md`.

## C2 — 가드 구현 (8편집: code-pair.js 5 + SKILL.md 3)

| # | 내용 |
|---|------|
| pre-flight | `stepSpec.estimatedNewBodyLines`(Lead 추정) > 600 → 스폰 전 `mode:'split_required'` 반환 (27분 낭비 회피) |
| null 재정의 | Stage1 null → fallback 유지 + `splitSuggested`(files≥4 OR complexity=5). "일시 장애=재시도 / Lead=fable SOLO=사용자 승인" (H5 우려② 해소) |
| post-warn | 실제 newBody 합산 > 600 시 WARN log (반환 불변) |
| 헤더 계약 | 반환 mode에 `split_required` 추가 |
| 소비처 4곳 | code-pair 헤더 + fz-code/fz-fix/fz SKILL 반환 사다리(split 우선 → 재시도 → 사용자승인 SOLO) + arg 조립에 estimatedNewBodyLines 명문화 |

**split_required = 하드 차단 아닌 Lead 판단 요구.** 임계는 상수 노출(관측 실패에서 재조정).

## C3 — 검증

- node --check EXIT=0 · lint-model-explicit PASS(fable=3) · 격리 스모크 **10/10**(over-decomposition 방지·하위호환·null≠과대 구분)
- **Codex check(자기참조 blind spot 방어) P2 2건 반영**:
  - (a) arg 문서에 `estimatedNewBodyLines` 누락 → 정상 경로에서 가드 dead → fz-code/fz-fix arg 조립에 명문화
  - (b) null 경로 `est !== null` 프록시 → 작은 Step 일시장애 오분류(est>임계는 pre-flight가 이미 조기 반환) → 제거

## 의의

- 코드 생산 = opus Workflow(compose+review pass), Lead(fable) = 적용·게이트 (위임 규율)
- 우리 자신의 실패가 설계 입력: B1 세션한도 null을 "과대 changeset"으로 오분류하지 않도록 프록시 설계
- **하네스 홀 H5 조치 완료** → H1~H5/F5/F6 중 미구현 0 (H4는 서베이가 보류 지지)

## 전체 (Wave A+B+C 완료)

하네스 서베이 2026 분석 → proposal 13주제 → plan-final → Wave A(가이드 v4.19.0)·B(메커니즘 v4.20.0)·C(H5 가드 v4.21.0). 산출물 아카이브 `~/dev/TVING/harness-paper/`.
