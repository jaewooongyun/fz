# Release v4.11.0 (2026-06-04)

**Title**: Opus 4.8 가이드 정합 + 인용 위생 + 하네스 구조 개선
**Type**: MINOR
**Trigger**: "fz 플러그인 고도화" 세션 — deep-research(인용 실재성 감사 + 신규 권위 출처) → 가이드/인용 정정 → Opus 4.8 출시 정합 → 리뷰 기반 구조 개선.
**Cost note**: Codex(GPT) 할당량 초과 → 동종 Claude 다관점(workflow 5-lens ×2) + 공식 docs verbatim 대조로 대체. cross-model 재검증은 복구 시 권장 (메모리 23차).

## TL;DR

fz 가이드를 **Opus 4.8 공식 사양에 정합화**(4.7 내용 제거 + effort 기본 high·자기결함 ~4x↓·tool 효율↑·수백 parallel subagents 큐레이션) + **arXiv 원문 대조 인용 수치 정정**(MAST 67→36.94%, OpenDev 7→8/6→7, Context Rot, NLAH subset) + **하네스 구조 개선**(운영점 원칙7, DELETE/MERGE-default rule, parallelizable modifier, A3). 3 기능단위 커밋, 16파일 +80/-51. breaking change 0.

## Highlights

### 1. Opus 4.8 가이드 정합 (feat)

공식 `anthropic.com/news/claude-opus-4-8` verbatim 기반:

- **검증된 4.8 사실** (harness §10 model-gen 표 + CLAUDE.md): effort **기본 high**(xhigh/max는 더 어려운 작업) / **자기 코드 결함 통과 ~4x↓**(self-eval 개선) / **tool-calling 효율↑**(required-call skip↓) / **단일 세션 수백 parallel subagents**.
- **fz 적용 큐레이션** (§10 4-bullet): effort는 세션 게이팅 대상 아님(원칙7) / self-eval 개선분 인정 + cross-model은 이종 blind-spot 보완 / tool 게이트 완화 / breadth fan-out 공식 지지.
- **4.7 제거**: length-limit(04-16 적용→04-20 철회, 4.6·4.7 ~3% 성능저하 "wrong tradeoff") · Cobus "harness release" · partially-verified 태그. §8 "literal"(4.7 community 용어)→"instruction-following consistency"(4.8 공식). 모델 참조 `claude-opus-4-8`.
- ⛔ **제거된 미검증 주장**(공식 fetch가 배치 확인): "fewer subagents"(공식은 "수백 parallel subagents")·"SWE-Pro 69.2%"(공식 미기재)·"literal"(공식은 "consistency"). 이전 세션 `[미검증: wp0rdknnz]`였던 주장들.

### 2. 인용 위생 (fix) — arXiv v3 원문 대조

- **MAST** (2503.13657 v3): FC2 inter-agent 67%→**36.94%** (FC1 41.77/FC3 21.30, 논문 명시 "단일 지배 카테고리 없음 — balanced"). FM-2.2 **6.80%** + MAST-Data **7개 프레임워크/1642 traces**(기존 "28 시스템" 정정). 4곳 active 인용.
- **OpenDev** (2603.05344): 도구 카테고리 7→8(§2.4) · 서브시스템 6→7(§2.3) · "instruction fade-out" 턴수치 제거(논문 미명시).
- **NLAH** (2603.25723): IHR ~74%에 benchmark subset caveat.
- **Context Rot**: "64K 전모델 저하"→"보편 임계값 없음(Chroma 명시)".
- `codex-skills/fz-challenger`의 `[verified] 67% 가장 치명적` 모순 해소.

### 3. 하네스 구조 개선 (feat)

- **harness §5 원칙7 (운영점)**: `effort`(추론 깊이)≠`SOLO/TEAM`(에이전트 수) 레버 구분 — SOLO 게이팅은 fan-out↓이지 추론깊이↓ 아님. max+ultracode 운영점 함의(token비용 세션 비제약 / surface 축소 / Codex-leg effort 보존).
- **skill-authoring WS4 — DELETE/MERGE-default**: 스킬 편집 시 DELETE/MERGE 우선 검토, additive 변경은 counterpart 필수 (IFScale 500규칙→68% 근거, additive-only 방어).
- **complexity parallelizable modifier**: coupled 작업(대부분 구현/리팩토링)은 fan-out 대신 single-thread 구성 + 다관점 검증. 5차원 점수 대체 아닌 실행형태 조정.
- **cross-validation A3**: 동종 모델 합의 ≠ 독립검증 — cross-model(Codex) 후 신뢰, lone correct dissent > majority.
- harness Index §7/§11 ref 정정.

## 검증

- 3 기능단위 커밋, hunk 단위 분리(공유 가이드 4개의 3기능 hunk를 `git apply --cached`로 분리, staged 누출 0 검증).
- 전수 verify: `claude-opus-4-7` URL 0 / 미검증 주장 0 / peer-review Gate ID(4.6/4.7/4.8) 보존 / 모델 "4.7" = README historical 1건뿐.
- pre-commit hook(user-path 차단) 통과.

## Breaking changes

없음 (모두 가이드/문서 갱신 + 인용 정정).

## 알려진 제약

- **cross-model 검증 부재**: Codex 할당량 초과로 동종 Claude 검증만 수행. A3 규칙상 독립검증 아님 — Codex 복구 시 재검증 권장.
- MEMORY.md(사용자 도메인)의 "§7 Ablation" 참조 부정확은 note만 (편집 안 함).
