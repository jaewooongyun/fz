{
  "_comment": "도구·서버 선언 인벤토리 — S3·S6·S20·#12 의 단일 출처 (C1/N4).",
  "_why": "N4: '/sc:·Serena·LSP·JIRA 를 세 서버째 손으로 고쳤고 검사는 없다'. 목록이 소비자마다 흩어져 있으면 한 곳만 고쳐진다(같은 사실의 선언처가 여럿).",
  "_update_method": [
    "MCP 서버 추가/개명 시: (1) mcp_servers 에 추가 (2) 개명이면 retired_mcp_prefixes 에 옛 접두 → 새 이름 (3) `python3 scripts/lint_contracts.py` 로 #12 확인",
    "실사용 서버 전수는 이 명령으로 뽑는다: grep -rhoE 'mcp__(plugin_[a-z-]+_)?[a-z0-9_-]+__' agents skills modules | sort -u",
    "외부 명령 추가 시: external_commands 에 {required} 와 함께 추가 → `python3 scripts/check_external_commands.py`",
    "⛔ 이 파일은 **선언**이다. 실제 가용성(서버가 떠 있는가)은 별개 축이며 lint_contracts #2(SEMANTIC)가 소관이다 — 정적으로 닫히지 않는다."
  ],
  "_measured": "2026-09-22 — 실사용 MCP 서버 6종 · 외부 명령 17종",

  "mcp_servers": {
    "plugin_fz_serena": { "form": "plugin", "note": "플러그인 제공 — 런타임이 `plugin_fz_` 접두를 붙인다" },
    "codegraph":        { "form": "plain",  "note": "참조·영향 반경 Primary" },
    "context7":         { "form": "plain",  "note": "라이브러리 문서" },
    "sequential-thinking": { "form": "plain", "note": "단계 추론" },
    "atlassian-jira":   { "form": "plain",  "note": "REST path 를 받는 범용 도구" },
    "github":           { "form": "plain",  "note": "PR·이슈" }
  },

  "retired_mcp_prefixes": {
    "mcp__serena__":    "mcp__plugin_fz_serena__ (v4.35.0 — 런타임은 플러그인 접두를 붙인다)",
    "mcp__lsp__":       "네이티브 LSP 도구 (v4.36.0 — 그런 이름의 MCP 서버가 없다)",
    "mcp__atlassian__": "mcp__atlassian-jira__ (v4.36.0 — REST path 를 받는 범용 도구)"
  },

  "external_commands": {
    "python3": { "required": true,  "note": "전 스크립트" },
    "git":     { "required": true,  "note": "게이트·릴리즈·baseline" },
    "grep":    { "required": true,  "note": "검사 전반" },
    "bash":    { "required": true },
    "codex":   { "required": false, "note": "/fz-gpt — 부재 시 fresh-context Agent 폴백이 정의돼 있다" },
    "jq":      { "required": false, "note": "일부 게이트 예시" },
    "mkdir":   { "required": true }, "cat":  { "required": true },
    "printf":  { "required": true }, "xargs": { "required": true },
    "sleep":   { "required": true }, "test": { "required": true },
    "command": { "required": true }, "find": { "required": true },
    "sed":     { "required": true }, "sort": { "required": true },
    "wc":      { "required": true }
  }
}
